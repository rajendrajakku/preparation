const databaseData = [
{q:"What is a relational database?", a:"A database that organizes data into tables (rows and columns) with defined relationships between tables, enforced through keys, and queried using SQL."},
{q:"What is SQL?", a:"Structured Query Language — the standard language used to create, read, update, and delete data in a relational database, and to define its structure."},
{q:"What is a primary key?", a:"A column (or set of columns) that uniquely identifies each row in a table; it cannot be null and must be unique across all rows."},
{q:"What is a foreign key?", a:"A column in one table that references the primary key of another table, enforcing a relationship between the two and preventing orphaned/invalid references."},
{q:"What is the difference between a primary key and a unique key?", a:"A table can have only one primary key, and it cannot be null. A table can have multiple unique keys, and unique keys can allow one null value (in most databases)."},
{q:"What is a composite key?", a:"A primary key made up of two or more columns together, used when no single column alone is sufficient to uniquely identify a row."},
{q:"What is normalization?", a:"The process of organizing database tables to reduce data redundancy and improve data integrity, typically by splitting data into related tables following defined normal forms."},
{q:"What is First Normal Form (1NF)?", a:"Requires each column to hold only atomic (indivisible) values and each row to be unique — no repeating groups or multiple values crammed into a single column."},
{q:"What is Second Normal Form (2NF)?", a:"Requires the table to already be in 1NF, plus every non-key column must depend on the entire primary key, not just part of a composite key."},
{q:"What is Third Normal Form (3NF)?", a:"Requires the table to already be in 2NF, plus no non-key column depends on another non-key column (eliminating transitive dependencies)."},
{q:"What is denormalization, and when would you use it?", a:"Intentionally introducing some redundancy back into a normalized schema, typically to improve read performance by reducing the number of joins needed — used when read speed matters more than storage efficiency and update complexity."},
{q:"What is a JOIN in SQL?", a:"An operation that combines rows from two or more tables based on a related column between them, letting you query data that spans multiple tables in one result set."},
{q:"What is the difference between INNER JOIN and LEFT JOIN?", a:"INNER JOIN returns only rows that have matching values in both tables. LEFT JOIN returns all rows from the left table, with matching data from the right table where it exists, and NULLs where it doesn't.", ex:"SELECT o.id, c.name FROM orders o LEFT JOIN customers c ON o.customer_id = c.id;", code:true},
{q:"What is the difference between LEFT JOIN and RIGHT JOIN?", a:"LEFT JOIN keeps all rows from the left (first-listed) table. RIGHT JOIN keeps all rows from the right (second-listed) table — they're functionally mirror images of each other."},
{q:"What is a FULL OUTER JOIN?", a:"Returns all rows from both tables, matching where possible, and filling in NULLs for the columns on whichever side has no match."},
{q:"What is a self join, and when would you use one?", a:"A table joined to itself, useful for comparing rows within the same table — like finding employees who earn more than their manager, where both employee and manager are rows in the same employees table.", ex:"SELECT e.name FROM employees e JOIN employees m ON e.manager_id = m.id WHERE e.salary > m.salary;", code:true},
{q:"What is a CROSS JOIN?", a:"Returns the Cartesian product of two tables — every row from the first table paired with every row from the second, resulting in rows equal to the product of both table sizes. Rarely used intentionally except for generating combinations."},
{q:"What is the difference between WHERE and HAVING?", a:"WHERE filters individual rows before any grouping happens. HAVING filters groups after a GROUP BY has been applied, since aggregate functions like COUNT() or SUM() aren't available to filter on until after grouping.", ex:"SELECT dept, COUNT(*) FROM employees GROUP BY dept HAVING COUNT(*) > 5;", code:true},
{q:"What is GROUP BY used for?", a:"Groups rows sharing the same value in specified columns, typically used together with aggregate functions (COUNT, SUM, AVG) to summarize data per group."},
{q:"What is the difference between COUNT(*), COUNT(column), and COUNT(DISTINCT column)?", a:"COUNT(*) counts all rows, including nulls. COUNT(column) counts non-null values in that column. COUNT(DISTINCT column) counts unique non-null values in that column."},
{q:"What is an index in a database?", a:"A separate data structure (commonly a B-tree) that stores sorted references to table rows, letting the database find rows matching a condition much faster than scanning the entire table."},
{q:"Why does adding an index sometimes slow down write operations (INSERT/UPDATE/DELETE)?", a:"Because every index on a table needs to be updated whenever a row is inserted, updated, or deleted, adding overhead to write operations — more indexes speed up reads but slow down writes."},
{q:"What is the difference between a clustered index and a non-clustered index?", a:"A clustered index determines the actual physical order data is stored on disk (a table can have only one). A non-clustered index is a separate structure with pointers back to the actual rows (a table can have many)."},
{q:"What is a composite index, and when is it useful?", a:"An index built on multiple columns together, useful when queries frequently filter or sort by that same combination of columns — the column order in the index matters for which query patterns it can efficiently support."},
{q:"How do you decide which columns to index?", a:"Index columns frequently used in WHERE clauses, JOIN conditions, and ORDER BY clauses — especially on large tables with selective values (many distinct values), while avoiding excessive indexing on tables with heavy write traffic."},
{q:"What is a full table scan, and why is it usually something to avoid on large tables?", a:"When the database reads every row in a table to find matches, because no suitable index exists for the query condition. It's slow on large tables and usually a sign that an appropriate index is missing."},
{q:"What is EXPLAIN (or EXPLAIN PLAN), and why is it useful?", a:"A command that shows how the database engine intends to execute a query — which indexes it will use, in what order tables are joined, and whether it expects a full table scan — critical for diagnosing slow queries.", ex:"EXPLAIN SELECT * FROM orders WHERE customer_id = 42;", code:true},
{q:"What is a database transaction?", a:"A sequence of one or more database operations executed as a single logical unit — either all operations succeed and are committed, or if any fails, all are rolled back, leaving the database unchanged."},
{q:"What does ACID stand for?", a:"Atomicity (all-or-nothing), Consistency (data moves between valid states only), Isolation (concurrent transactions don't interfere with each other), and Durability (once committed, changes survive even a crash)."},
{q:"What is the difference between COMMIT and ROLLBACK?", a:"COMMIT permanently saves all changes made during the current transaction. ROLLBACK undoes all changes made during the current transaction, reverting to the state before it began."},
{q:"What are the different transaction isolation levels?", a:"Read Uncommitted (can see uncommitted changes from other transactions — dirty reads possible), Read Committed (only sees committed data), Repeatable Read (guarantees the same data if read twice within a transaction), and Serializable (strictest — transactions behave as if executed one at a time)."},
{q:"What is a dirty read?", a:"When a transaction reads data that another transaction has changed but not yet committed — if that other transaction rolls back, the data read was never actually valid."},
{q:"What is a non-repeatable read?", a:"When a transaction reads the same row twice and gets different values because another transaction modified and committed a change to that row in between the two reads."},
{q:"What is a phantom read?", a:"When a transaction re-runs a query and gets a different set of rows (not just changed values) because another transaction inserted or deleted rows matching that query's condition in between."},
{q:"What is deadlock in a database context, and how is it typically resolved?", a:"When two transactions each hold a lock the other needs, waiting on each other indefinitely. Most databases automatically detect this and forcibly roll back one of the transactions (the 'deadlock victim') to break the cycle."},
{q:"What is the difference between optimistic and pessimistic locking?", a:"Pessimistic locking locks a row immediately upon reading it for update, blocking other transactions until it's released. Optimistic locking doesn't lock on read; instead, it checks (often via a version column) at update time whether the data changed since it was read, failing the update if so."},
{q:"What is a database view?", a:"A saved, virtual table defined by a SQL query — it doesn't store data itself but presents data from underlying tables, useful for simplifying complex queries or restricting access to specific columns/rows."},
{q:"What is the difference between a view and a materialized view?", a:"A regular view runs its underlying query fresh every time it's accessed. A materialized view stores the query's result physically and must be refreshed periodically, trading data freshness for much faster read performance."},
{q:"What is a stored procedure?", a:"A precompiled block of SQL/procedural logic stored in the database itself, which can be called by name — useful for encapsulating complex business logic close to the data and reducing repeated network round-trips."},
{q:"What are the pros and cons of using stored procedures vs. application-layer logic?", a:"Pros: can reduce network round-trips, keep logic centralized for multiple applications. Cons: harder to version control and test compared to application code, ties business logic tightly to a specific database vendor, and can make logic harder for developers to discover and maintain."},
{q:"What is a database trigger?", a:"A piece of logic that automatically executes in response to a specific event (INSERT, UPDATE, DELETE) on a table, often used for auditing, enforcing complex constraints, or maintaining derived data."},
{q:"What is a subquery?", a:"A query nested inside another query, used to compute an intermediate result that the outer query then uses, e.g., in a WHERE clause or as a derived table.", ex:"SELECT name FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);", code:true},
{q:"What is the difference between a correlated and a non-correlated subquery?", a:"A non-correlated subquery runs independently once and its result is used by the outer query. A correlated subquery references a column from the outer query and re-runs once for every row the outer query processes — often slower."},
{q:"What is a Common Table Expression (CTE), and why use one?", a:"A named temporary result set defined with WITH, usable within a single query, making complex queries more readable by breaking them into named logical steps, and enabling recursive queries.", ex:"WITH high_earners AS (SELECT * FROM employees WHERE salary > 100000) SELECT * FROM high_earners WHERE dept = 'Engineering';", code:true},
{q:"What is the difference between UNION and UNION ALL?", a:"UNION combines results from two queries and removes duplicate rows, requiring extra processing. UNION ALL combines results and keeps duplicates, which is faster since no deduplication step is needed."},
{q:"What is query optimization, and what are common ways to improve a slow query?", a:"The process of making a query run faster and more efficiently. Common techniques: add appropriate indexes, avoid SELECT * (fetch only needed columns), avoid functions on indexed columns in WHERE clauses, rewrite correlated subqueries as joins where possible, and check the execution plan to find the actual bottleneck."},
{q:"Why does using a function on an indexed column in a WHERE clause hurt performance?", a:"It usually prevents the database from using the index on that column, since the index is built on the raw column values, not the function's output — forcing a full table scan instead. Example: WHERE YEAR(order_date) = 2024 typically can't use an index on order_date the way WHERE order_date >= '2024-01-01' can."},
{q:"What is connection pooling, and why does it matter for database performance?", a:"Reusing a pool of already-open database connections instead of opening and closing a new connection for every request, since establishing a connection is relatively expensive — pooling significantly reduces overhead under load."},
{q:"What is database sharding?", a:"Splitting a large database's data horizontally across multiple separate database instances (shards), typically by a key like customer ID range, so no single database instance has to hold or serve all the data alone."},
{q:"What is the difference between vertical and horizontal database scaling?", a:"Vertical scaling adds more resources (CPU, RAM, faster disk) to a single database server. Horizontal scaling adds more database servers and distributes data/load across them (sharding or read replicas)."},
{q:"What is a read replica, and why use one?", a:"A copy of a database kept in sync with the primary, used to serve read-only queries — offloading read traffic from the primary database so it can focus on handling writes, improving overall throughput."},
{q:"What is replication lag, and why does it matter?", a:"The delay between a write happening on the primary database and that change appearing on a read replica. It matters because an application reading from a replica right after writing to the primary might briefly see stale data."},
{q:"What is the difference between SQL and NoSQL databases?", a:"SQL databases are relational, with a fixed schema and tables related via keys, optimized for complex queries and strong consistency. NoSQL databases (document, key-value, column, graph) offer flexible/schema-less data models, often trading strict consistency for horizontal scalability and flexibility."},
{q:"When would you choose a NoSQL database over a relational one?", a:"When data doesn't fit a rigid relational schema well (e.g., deeply nested or highly variable documents), when you need to scale writes horizontally across many servers easily, or when eventual consistency is acceptable in exchange for speed and scale."},
{q:"What is a document database (e.g., MongoDB), and how does it differ from a relational database?", a:"Stores data as flexible, JSON-like documents rather than rows in fixed-schema tables, so related data can be nested within one document instead of requiring joins across separate tables — good fit when your application naturally works with whole objects."},
{q:"What is a key-value store (e.g., Redis), and what is it typically used for?", a:"A simple database storing data as key-value pairs with very fast lookups, commonly used for caching, session storage, or rate-limiting counters rather than as a primary system of record for complex relational data."},
{q:"What is database connection timeout, and why should you configure it explicitly?", a:"How long the application waits when trying to establish or use a database connection before giving up. Configuring it explicitly (rather than relying on a default) prevents requests from hanging indefinitely if the database is slow or unreachable."},
{q:"What is the purpose of a database migration tool (e.g., Flyway or Liquibase)?", a:"Manages incremental, versioned changes to a database schema in a controlled, repeatable, and trackable way across environments (dev, test, prod), instead of manually running ad hoc SQL scripts."},
{q:"What is the difference between DDL, DML, and DCL in SQL?", a:"DDL (Data Definition Language) defines structure — CREATE, ALTER, DROP. DML (Data Manipulation Language) manipulates data — SELECT, INSERT, UPDATE, DELETE. DCL (Data Control Language) manages permissions — GRANT, REVOKE."},
{q:"What is the difference between DELETE, TRUNCATE, and DROP?", a:"DELETE removes specific rows (can be filtered with WHERE) and can be rolled back. TRUNCATE removes all rows quickly but keeps the table structure, and is harder or impossible to roll back depending on the database. DROP removes the entire table structure and its data permanently."},
{q:"What is a check constraint?", a:"A rule enforced at the database level restricting what values are allowed in a column, e.g., CHECK (age >= 18), rejecting any insert/update that would violate it."},
{q:"What is referential integrity?", a:"A guarantee that relationships between tables (via foreign keys) remain valid — you can't reference a row in another table that doesn't exist, and the database enforces this automatically once foreign key constraints are defined."},
{q:"What happens when you try to delete a row that's referenced by a foreign key in another table?", a:"By default, the database rejects the delete to protect referential integrity, unless a cascading behavior (like ON DELETE CASCADE) has been explicitly configured to automatically delete or nullify related rows."},
{q:"What is the difference between ON DELETE CASCADE, SET NULL, and RESTRICT?", a:"CASCADE automatically deletes dependent rows when the referenced row is deleted. SET NULL sets the foreign key column to null in dependent rows instead. RESTRICT (the typical default) blocks the delete entirely if dependent rows exist."},
{q:"What is a database schema?", a:"The overall structural blueprint of a database — its tables, columns, data types, relationships, and constraints — defining how data is organized."},
{q:"What is the difference between a database and a schema?", a:"In many database systems, a schema is a logical namespace/grouping of tables within a database, letting you organize related tables together and manage access separately, while a database is the broader container that can hold one or more schemas."},
{q:"What is SQL injection, and how do you prevent it?", a:"An attack where malicious input is crafted to alter the intended SQL query, potentially exposing or corrupting data. Prevented by always using parameterized queries/prepared statements instead of concatenating user input directly into SQL strings.", ex:"// Vulnerable: \"SELECT * FROM users WHERE name = '\" + input + \"'\"\n// Safe: PreparedStatement with a '?' placeholder bound to input", code:true},
{q:"What is a prepared statement, and why does it prevent SQL injection?", a:"A precompiled SQL query template with placeholders for parameters, where the database treats supplied values strictly as data — not executable SQL — eliminating the possibility of injected SQL syntax altering the query's meaning."},
{q:"What is database normalization's tradeoff with query performance?", a:"Highly normalized schemas reduce redundancy and improve data integrity but often require more joins to reconstruct a full picture of related data, which can be slower for read-heavy workloads — hence denormalization is sometimes used deliberately for performance."},
{q:"How would you diagnose a slow-running query in production?", a:"Run EXPLAIN to see the execution plan, check whether expected indexes are actually being used, look at how many rows are being scanned vs returned, check for locking/blocking from other transactions, and review recent data growth that might have changed the optimal query plan."},
{q:"What is a covering index?", a:"An index that includes all the columns a query needs, letting the database satisfy the query entirely from the index itself without needing to look up the actual table rows — significantly faster for that specific query."},
{q:"What is pagination in SQL, and how is it typically implemented?", a:"Fetching results in smaller chunks ('pages') rather than the entire result set at once, typically implemented with LIMIT and OFFSET (or equivalent), often combined with an ORDER BY to ensure consistent page ordering.", ex:"SELECT * FROM orders ORDER BY id LIMIT 20 OFFSET 40;", code:true},
{q:"Why can OFFSET-based pagination become slow on large tables, and what's an alternative?", a:"With a large OFFSET, the database still has to scan and discard all the skipped rows before returning the requested page, which gets slower as the offset grows. Keyset (cursor-based) pagination — filtering by 'where id > last_seen_id' — avoids this since it can jump directly using an index."},
{q:"What is the difference between a natural key and a surrogate key?", a:"A natural key is a real-world unique attribute (like an email or SSN) used as the primary key. A surrogate key is an artificially generated identifier (like an auto-incrementing integer or UUID) with no business meaning, often preferred since natural keys can sometimes change or aren't guaranteed unique long-term."},
{q:"What is the purpose of using UUIDs instead of auto-incrementing integers as primary keys?", a:"UUIDs can be generated independently by any service or client without coordinating with a central database sequence, which is valuable in distributed systems, though they take more storage space and can hurt index locality compared to sequential integers."},
{q:"What is a database backup strategy, and why does it matter for a production system?", a:"A defined plan for regularly saving copies of the database (full and incremental backups) so data can be restored after corruption, accidental deletion, or hardware failure — critical for any production system handling important business data."},
{q:"What is the difference between a full backup and an incremental backup?", a:"A full backup captures the entire database at a point in time. An incremental backup captures only the changes since the last backup, making it faster and smaller, but requiring the full backup plus a chain of incrementals to fully restore."},
{q:"What is database partitioning (not to be confused with sharding across separate servers)?", a:"Splitting a very large table into smaller physical pieces (partitions) within the same database instance, typically by a range (like date) or list of values, improving query performance and maintenance operations on that specific subset of data."},
{q:"How would you design a database schema for an e-commerce order system, at a high level?", a:"Separate tables for Customers, Products, Orders, and OrderItems, with OrderItems as a join table capturing which products (and quantities/prices) belong to which order — foreign keys enforcing that every order references a valid customer and every order item references a valid order and product."},
{q:"What is the difference between a many-to-many relationship and how it's implemented in SQL?", a:"A many-to-many relationship (e.g., students and courses, where each student takes many courses and each course has many students) can't be represented with a simple foreign key — it requires a separate junction/join table holding foreign keys to both related tables."},
{q:"What is an ORM (Object-Relational Mapper), and what problem does it solve?", a:"A library (like Hibernate) that maps database rows to application objects automatically, letting developers work with objects instead of writing raw SQL for every operation, though it can sometimes hide performance-relevant details like the actual generated queries."},
{q:"What is the N+1 query problem from a pure SQL/database perspective?", a:"Executing one query to fetch a list of parent rows, then a separate additional query for each parent row to fetch related child data — resulting in N+1 total queries instead of one efficient join or batched query, significantly hurting performance at scale."},
{q:"What is query caching, and what's a risk of using it?", a:"Storing the result of a query so a repeated identical query can be served without hitting the database again. The risk is serving stale data if the underlying data changes and the cache isn't properly invalidated in time."},
{q:"What is the difference between a hot backup and a cold backup?", a:"A hot backup is taken while the database is still running and accepting transactions, without downtime. A cold backup requires the database to be shut down first, ensuring a perfectly consistent snapshot but causing downtime."},
{q:"What is database sharding's biggest challenge?", a:"Handling queries or transactions that need data spanning multiple shards (like a join across shard boundaries) becomes complex, since a single query can no longer be resolved by one database instance alone."},
{q:"What is the CAP theorem, and how does it relate to database design choices?", a:"States that a distributed data system can only guarantee two of three properties at once during a network partition: Consistency (all nodes see the same data), Availability (every request gets a response), and Partition tolerance (the system keeps working despite network failures). It explains why many distributed databases explicitly choose to favor availability with eventual consistency, or strict consistency at the cost of availability during a partition."},
{q:"What is the difference between strong consistency and eventual consistency?", a:"Strong consistency guarantees that any read immediately reflects the latest write. Eventual consistency guarantees that, given enough time without new writes, all replicas will converge to the same value — but a read shortly after a write might briefly return stale data."},
{q:"What is a materialized path or nested set model, and when might it be used?", a:"Techniques for representing hierarchical (tree-like) data in a relational database (like a category tree) in a way that makes certain queries (like 'find all descendants') faster than a naive recursive parent-id lookup, at the cost of more complex updates."},
{q:"What is the difference between a scalar function and an aggregate function in SQL?", a:"A scalar function operates on a single value and returns a single value per row (e.g., UPPER(), ROUND()). An aggregate function operates across multiple rows and returns one summarized value per group (e.g., SUM(), AVG(), COUNT())."},
{q:"What is the purpose of the DISTINCT keyword in SQL?", a:"Removes duplicate rows from a query's result set, returning only unique combinations of the selected columns."},
{q:"What is a window function in SQL, and how is it different from a GROUP BY aggregate?", a:"A window function (like ROW_NUMBER(), RANK(), or SUM() OVER()) performs a calculation across a set of related rows without collapsing them into a single summarized row like GROUP BY does — each original row stays in the result, with the calculated value attached.", ex:"SELECT name, salary, RANK() OVER (ORDER BY salary DESC) as rank FROM employees;", code:true},
{q:"How would you find the second-highest salary using SQL?", a:"Either use a subquery finding the max salary less than the overall max, or use a window function like DENSE_RANK() and filter for rank 2 — the window function approach handles ties more predictably.", ex:"SELECT MAX(salary) FROM employees WHERE salary < (SELECT MAX(salary) FROM employees);", code:true},
{q:"What is the difference between HAVING COUNT(*) > 1 and a subquery approach for finding duplicate rows?", a:"HAVING COUNT(*) > 1 after a GROUP BY is the standard, efficient approach — it groups by the column(s) you're checking for duplicates and filters groups occurring more than once, generally simpler and faster than an equivalent correlated subquery."},
{q:"What is the significance of choosing appropriate data types (e.g., using DATE instead of VARCHAR for dates)?", a:"Proper data types enforce valid data at the database level, allow efficient storage and indexing, and let the database perform correct comparisons/sorting (e.g., date arithmetic) — storing dates as text loses these benefits and invites data quality bugs."},
{q:"What is the difference between VARCHAR and CHAR data types?", a:"CHAR is fixed-length, padding shorter values with spaces to a set width. VARCHAR is variable-length, storing only the actual characters plus a small length indicator — generally preferred unless all values are genuinely the same fixed length."},
{q:"What is the purpose of a NOT NULL constraint?", a:"Ensures a column must always have a value, rejecting any insert or update that would leave it empty — used to enforce that critical business data is never missing."},
{q:"What is an execution plan cost estimate, and why might it be wrong?", a:"The database estimates the relative cost of different ways to execute a query based on statistics about table size and data distribution. These estimates can be wrong if table statistics are outdated (e.g., after a large bulk load), leading the optimizer to choose a suboptimal plan until statistics are refreshed."},
{q:"What is the purpose of running ANALYZE (or updating statistics) on a table periodically?", a:"Refreshes the database's internal statistics about data distribution in a table, helping the query optimizer make better decisions about execution plans, especially after significant data changes."},
{q:"What is the difference between an implicit and explicit transaction in SQL?", a:"An implicit transaction is automatically started and committed by the database for each individual statement unless told otherwise (autocommit mode). An explicit transaction is manually started (BEGIN TRANSACTION) and ended (COMMIT/ROLLBACK) by the application, allowing multiple statements to be grouped into one atomic unit."},
{q:"How would you safely perform a large data migration or bulk update on a production table without locking it for too long?", a:"Break the update into smaller batches (e.g., updating a few thousand rows at a time) rather than one massive statement, run it during low-traffic periods, monitor for lock contention, and consider adding appropriate indexes temporarily if the batching query itself needs to scan efficiently."},
{q:"What is the purpose of foreign key indexing, and is it automatic?", a:"Indexing foreign key columns speeds up joins and lookups on those relationships and helps avoid locking issues during cascading deletes/updates. In many databases (like MySQL/InnoDB), an index is automatically created for foreign keys, but this isn't universal — it's worth verifying explicitly, especially in PostgreSQL where it is not automatic."},
{q:"What is a database connection leak, and how does it typically happen in application code?", a:"When a database connection is opened but never properly closed/returned to the pool, usually due to a missing try-finally (or try-with-resources) around connection usage — over time this exhausts the connection pool, causing new requests to fail or hang."},
{q:"How would you explain your SQL query tuning work (30% response time reduction, from your CV) in an interview?", a:"Walk through the actual investigation: how you found the slow query (e.g., via EXPLAIN or monitoring), what the root cause was (missing index, N+1 pattern, inefficient join), the specific fix you applied, and how you measured and confirmed the improvement — concrete specifics are far more convincing than restating the headline number."}
];
