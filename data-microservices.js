const microservicesData = [
{q:"What is a microservices architecture?", a:"An approach to building an application as a collection of small, independently deployable services, each owning a specific business capability, communicating over the network instead of in-process function calls."},
{q:"How is microservices architecture different from a monolith?", a:"A monolith is one large deployable unit with all functionality bundled together and usually one shared database. Microservices split functionality into independently deployable, independently scalable services, typically each with its own database."},
{q:"What are the main benefits of microservices?", a:"Independent deployment (ship one service without redeploying everything), independent scaling (scale only the service under load), technology flexibility per service, and smaller codebases that are easier for teams to own."},
{q:"What are the main challenges/downsides of microservices?", a:"Distributed system complexity (network failures, latency), harder debugging across services, data consistency across separate databases, operational overhead (more services to deploy and monitor), and the need for strong DevOps/observability practices."},
{q:"When would you NOT recommend microservices for a project?", a:"For small teams, early-stage products, or simple applications where the operational complexity of a distributed system outweighs the benefits — a monolith is often faster to build and easier to reason about until real scaling or team-boundary pain shows up."},
{q:"What is a bounded context?", a:"A Domain-Driven Design concept describing the boundary within which a specific business domain model applies consistently — microservices are often designed around bounded contexts so each service owns a clear, non-overlapping piece of the business."},
{q:"How do you decide how to split a monolith into microservices?", a:"Identify natural business capability boundaries (e.g., Orders, Payments, Inventory) rather than splitting by technical layer, look for data that's owned distinctly by one capability, and split along places with the least coupling and clearest ownership."},
{q:"What is service discovery, and why is it needed?", a:"A mechanism letting services find each other's network locations dynamically, since in cloud/container environments IP addresses and ports change frequently as instances scale up/down or restart."},
{q:"What is the difference between client-side and server-side service discovery?", a:"In client-side discovery, the calling service queries a registry directly and picks an instance itself. In server-side discovery, the client calls a load balancer/router which queries the registry and forwards the request, keeping discovery logic out of the client."},
{q:"What is an API Gateway?", a:"A single entry point that sits in front of multiple microservices, handling cross-cutting concerns like routing, authentication, rate limiting, and request aggregation, so clients don't need to know about or call each service directly."},
{q:"What are the benefits of using an API Gateway?", a:"Centralizes authentication/authorization, hides internal service topology from clients, can aggregate multiple service calls into one client response, and provides a single place for cross-cutting concerns like logging and rate limiting."},
{q:"What is the Circuit Breaker pattern, and why is it critical in microservices?", a:"It 'trips' and stops calling a failing downstream service after repeated failures, failing fast instead of piling up slow/failing requests — preventing one struggling service from cascading failure through the whole system."},
{q:"What are the states of a Circuit Breaker?", a:"Closed (requests flow normally, failures are counted), Open (requests fail immediately without calling the downstream service, giving it time to recover), and Half-Open (a limited number of test requests are allowed through to check if the service has recovered)."},
{q:"What is the difference between synchronous and asynchronous communication between microservices?", a:"Synchronous communication (like REST calls) waits for an immediate response, coupling the caller to the callee's availability. Asynchronous communication (like message queues) lets the caller continue without waiting, decoupling services in time."},
{q:"What is a message broker, and why use one between microservices?", a:"Software (like Kafka or RabbitMQ) that lets services communicate by publishing and consuming messages instead of calling each other directly, decoupling services so a producer doesn't need the consumer to be available at the same moment."},
{q:"What is the difference between a message queue and a publish-subscribe (pub/sub) model?", a:"A queue typically delivers each message to exactly one consumer (point-to-point). Pub/sub broadcasts each message to all subscribers interested in that topic — many services can react to the same event independently."},
{q:"What is event-driven architecture?", a:"A design style where services communicate by emitting and reacting to events (e.g., 'OrderPlaced') rather than direct calls, allowing loosely coupled services to independently react to state changes elsewhere in the system."},
{q:"What is eventual consistency, and why is it common in microservices?", a:"A consistency model where data across services will become consistent over time, but not necessarily instantly, because each service typically owns its own database and updates propagate asynchronously — a tradeoff accepted for scalability and service independence."},
{q:"What is the Saga pattern?", a:"A way to manage data consistency across multiple services in a distributed transaction, by breaking it into a sequence of local transactions, each with a corresponding compensating action to undo it if a later step fails."},
{q:"What is the difference between orchestration-based and choreography-based Sagas?", a:"Orchestration uses a central coordinator that tells each service what to do and in what order. Choreography has no central coordinator — each service reacts to events from others and decides its own next action, which is more decoupled but harder to trace."},
{q:"Why can't microservices typically use a traditional ACID distributed transaction across services?", a:"Because each service usually owns its own database, and true distributed ACID transactions (like two-phase commit) are slow, don't scale well, and create tight coupling/availability dependencies between services — so Sagas and eventual consistency are used instead."},
{q:"What is database-per-service, and why is it a common microservices principle?", a:"Each microservice owns and exclusively accesses its own database, preventing other services from reaching in directly — this keeps services independently deployable and prevents tight coupling through a shared schema."},
{q:"What problem does sharing a single database across microservices cause?", a:"It recreates tight coupling — any schema change can break other services relying on that table, defeating the purpose of independent deployability that microservices are meant to provide."},
{q:"What is CQRS (Command Query Responsibility Segregation)?", a:"A pattern that separates the model used for writing data (commands) from the model used for reading data (queries), often using different, independently optimized data stores for each — useful when read and write patterns have very different scaling needs."},
{q:"What is the strangler fig pattern for migrating a monolith to microservices?", a:"Gradually replacing pieces of a monolith by routing specific functionality to new microservices over time, while the monolith keeps handling everything not yet migrated, avoiding a risky big-bang rewrite."},
{q:"What is service-to-service authentication, and how is it typically handled?", a:"Ensuring only legitimate services (not external attackers) can call internal service-to-service APIs, often done with mutual TLS (mTLS), API keys, or service tokens validated by each service or a service mesh."},
{q:"What is a service mesh (e.g., Istio)?", a:"Infrastructure layer that handles service-to-service communication concerns (routing, retries, mTLS, observability) outside of application code, typically via sidecar proxies deployed alongside each service instance."},
{q:"What is the sidecar pattern?", a:"Deploying a helper process (like a service mesh proxy) alongside a main application container, handling cross-cutting concerns (networking, logging) so the main application code doesn't need to implement them itself."},
{q:"What is distributed tracing, and why is it important in microservices?", a:"A technique for tracking a single request as it flows across multiple services, using a shared trace ID, so you can see the full path and timing of a request — essential for debugging performance issues and failures that span services."},
{q:"What tools are commonly used for distributed tracing?", a:"Zipkin, Jaeger, and OpenTelemetry are common — they collect timing and metadata (spans) from each service a request touches and stitch them into one trace."},
{q:"What is centralized logging, and why do microservices need it?", a:"Aggregating logs from all services into one searchable system (e.g., ELK stack — Elasticsearch, Logstash, Kibana), since with many independent services, checking logs on each individual instance separately is impractical when debugging an issue."},
{q:"What is a correlation ID, and why is it used across microservices?", a:"A unique identifier attached to a request when it first enters the system and passed along to every downstream service call it triggers, letting you trace and correlate all related log entries across services for that single request."},
{q:"What is the difference between horizontal and vertical scaling, and which does microservices architecture favor?", a:"Vertical scaling adds more resources (CPU/RAM) to a single instance. Horizontal scaling adds more instances of a service running in parallel. Microservices are designed to favor horizontal scaling, since stateless service instances can be added or removed easily behind a load balancer."},
{q:"What makes a microservice 'stateless', and why does it matter for scaling?", a:"A stateless service doesn't store client-specific data between requests in its own memory — any needed state is externalized (database, cache, token). This lets you add or remove instances freely without losing session data, since any instance can handle any request."},
{q:"What is container orchestration, and why is it used with microservices?", a:"Managing the deployment, scaling, networking, and health of many containerized services automatically (e.g., with Kubernetes), since manually managing dozens of independently deployed services would be impractical."},
{q:"What is Kubernetes, at a high level?", a:"A container orchestration platform that automates deploying, scaling, healing, and networking containerized applications across a cluster of machines."},
{q:"What is a Kubernetes Pod?", a:"The smallest deployable unit in Kubernetes, typically wrapping one application container (plus optional sidecars) that share networking and storage."},
{q:"What is the difference between liveness and readiness probes in Kubernetes?", a:"A liveness probe checks if a container is still running correctly; if it fails, Kubernetes restarts the container. A readiness probe checks if a container is ready to accept traffic; if it fails, Kubernetes stops routing requests to it without restarting it."},
{q:"What is blue-green deployment?", a:"A release strategy where two identical production environments exist ('blue' = current, 'green' = new version); traffic is switched over to green once it's verified healthy, allowing instant rollback by switching traffic back to blue if something's wrong."},
{q:"What is canary deployment?", a:"A release strategy where a new version is rolled out to a small subset of users/traffic first, monitored for issues, and gradually expanded to all traffic if it performs well — limiting the blast radius of a bad release."},
{q:"What is the difference between rolling deployment and blue-green deployment?", a:"Rolling deployment gradually replaces old instances with new ones one (or a few) at a time within the same environment. Blue-green keeps two full separate environments and switches traffic all at once, allowing an instant full rollback."},
{q:"How would you handle versioning when multiple microservices depend on each other's APIs?", a:"Use semantic versioning and backward-compatible changes wherever possible (adding optional fields, not removing existing ones), version the API explicitly when breaking changes are unavoidable, and use contract testing to catch incompatibilities before deployment."},
{q:"What is contract testing (e.g., Pact) in a microservices context?", a:"A testing approach where a consumer service defines the API contract it expects from a provider service, and that contract is verified against the actual provider automatically — catching breaking API changes before they hit production, without needing full end-to-end integration tests."},
{q:"How do you handle a scenario where Service A calls Service B, and Service B is slow or unresponsive?", a:"Set a strict timeout on the call so Service A doesn't hang indefinitely, use a Circuit Breaker to stop calling B if it keeps failing, consider a fallback response (cached/default data) if appropriate, and add retries with backoff only for transient failures, not for prolonged outages."},
{q:"What is the bulkhead pattern?", a:"Isolating resources (like thread pools or connection pools) used for calling different downstream services, so if one service starts failing or being slow, it doesn't exhaust resources needed to call other, healthy services — like separate compartments in a ship's hull."},
{q:"What is the difference between a load balancer and an API Gateway?", a:"A load balancer distributes incoming traffic across multiple instances of the same service for scaling/availability. An API Gateway sits at the edge, handling routing to different services, plus cross-cutting concerns like authentication — they're often used together."},
{q:"What is API composition, and when would you use it?", a:"Combining data from multiple microservices into a single response, either at the API Gateway or a dedicated aggregation service, useful when a client needs a combined view (e.g., an order with customer and shipping details) without making multiple round-trip calls itself."},
{q:"What is the Backend-for-Frontend (BFF) pattern?", a:"A dedicated backend service tailored to a specific client type (e.g., a mobile app or web app), aggregating and shaping data from multiple microservices in the exact format that particular frontend needs, rather than exposing raw generic APIs to every client type."},
{q:"How would you design a microservice to handle high transaction volume, based on your commodity trading background?", a:"Design for horizontal scalability with stateless service instances behind a load balancer, use asynchronous processing/message queues for non-blocking operations, add caching for frequently-read reference data, apply Circuit Breakers on downstream dependencies, and monitor with metrics/alerts to catch bottlenecks early."},
{q:"What is idempotency, and why does it matter specifically for microservices retries?", a:"An idempotent operation produces the same result no matter how many times it's executed. It matters because retries (after a timeout, when you're not sure if the first request succeeded) could otherwise cause duplicate processing, like charging a payment twice."},
{q:"How would you implement idempotency for a payment/order creation API?", a:"Require the client to send a unique idempotency key with the request; the server checks if that key has already been processed and, if so, returns the original result instead of processing the request again."},
{q:"What is the difference between REST and gRPC for service-to-service communication?", a:"REST typically uses JSON over HTTP and is human-readable and widely compatible. gRPC uses binary Protocol Buffers over HTTP/2, which is faster and more efficient for internal service-to-service calls, though less human-readable and needs generated client code."},
{q:"When would you choose gRPC over REST for internal microservice communication?", a:"When performance and low latency matter a lot (high-frequency internal calls), when you want strongly-typed contracts enforced by generated code, or when you need features like streaming — REST remains preferable for public-facing APIs due to broader compatibility and easier debugging."},
{q:"What is API rate limiting at the gateway level, and why apply it there rather than per-service?", a:"Restricting how many requests a client can make in a time window. Applying it at the gateway centralizes the logic in one place instead of duplicating rate-limiting code across every individual service."},
{q:"What is a dead letter queue, and why is it used with message-based microservices?", a:"A separate queue where messages are routed after repeatedly failing to be processed successfully, so they don't get stuck endlessly retrying or silently lost — allowing manual inspection and reprocessing later."},
{q:"What is the difference between at-most-once, at-least-once, and exactly-once message delivery?", a:"At-most-once may lose messages but never duplicates them. At-least-once guarantees delivery but might duplicate messages (requiring idempotent consumers). Exactly-once guarantees no loss and no duplication but is the hardest and most expensive to implement reliably."},
{q:"What is the outbox pattern?", a:"A pattern to reliably publish events when updating a database: instead of writing to the database and publishing a message as two separate, potentially inconsistent steps, you write the event to an 'outbox' table in the same local transaction, then a separate process reliably publishes it — avoiding lost or duplicated events from partial failures."},
{q:"How would you monitor the health of a microservices system in production?", a:"Combine health check endpoints, centralized metrics (request rates, error rates, latency — often via Prometheus/Grafana), centralized logging, distributed tracing, and alerting rules that notify the team when thresholds are breached."},
{q:"What are the four golden signals commonly monitored for services (from Google SRE practices)?", a:"Latency (how long requests take), traffic (how much demand there is), errors (rate of failed requests), and saturation (how full/loaded the service's resources are)."},
{q:"What is chaos engineering, and why would a microservices team practice it?", a:"Deliberately injecting failures (like killing an instance or adding network latency) into a system in a controlled way, to proactively discover weaknesses in resilience before they cause a real, unplanned outage."},
{q:"What is the difference between vertical partitioning and horizontal partitioning (sharding) of data in a microservices context?", a:"Vertical partitioning splits data by feature/domain (e.g., separate databases per service). Horizontal partitioning (sharding) splits the same type of data across multiple databases by a key (like customer ID range), used when a single service's data grows too large for one database."},
{q:"What is API throttling vs API rate limiting?", a:"They're closely related — rate limiting caps total requests over a period; throttling specifically slows down or queues excess requests rather than outright rejecting them, smoothing traffic spikes instead of hard-blocking them."},
{q:"What is service versioning at the API Gateway level, and how might it be implemented?", a:"Routing requests to different backend service versions based on the URL path (/v1/, /v2/) or a header, letting the gateway support multiple concurrent API versions while services are migrated gradually."},
{q:"How would you design zero-downtime deployments for a microservice?", a:"Use rolling or blue-green deployment strategies, ensure the service is stateless so any instance can serve any request, implement readiness probes so traffic isn't routed to instances still starting up, and keep API changes backward-compatible during the transition."},
{q:"What is the twelve-factor app methodology, and why is it relevant to microservices?", a:"A set of best practices for building cloud-native applications (e.g., config via environment variables, stateless processes, logs as event streams) that align closely with what makes microservices deployable, scalable, and portable across environments."},
{q:"What is the difference between an internal API and a public API in a microservices architecture?", a:"An internal API is used only for service-to-service communication within the system, often less strictly versioned and secured differently (e.g., mTLS). A public API is exposed to external clients through the gateway, requiring stricter versioning, documentation, authentication, and rate limiting."},
{q:"What is domain-driven design (DDD), and how does it relate to microservices boundaries?", a:"An approach to modeling software around the real business domain and its language, using concepts like bounded contexts and aggregates — microservices are frequently designed to align one service per bounded context, so technical boundaries mirror actual business boundaries."},
{q:"What is an aggregate in Domain-Driven Design?", a:"A cluster of related domain objects treated as a single unit for data changes, with one 'root' entity controlling access — ensures business rules and consistency are enforced whenever that cluster of data is modified."},
{q:"What is the difference between orchestrating microservices with a central workflow engine vs choreography via events — what are the tradeoffs?", a:"Orchestration gives clear visibility into the process flow and is easier to debug/modify centrally, but creates a central point of coupling. Choreography is more decoupled and scalable but harder to trace end-to-end and understand the overall business process just by looking at one service."},
{q:"How would you handle a shared reference data problem, like currency exchange rates needed by multiple services?", a:"Options include a dedicated 'reference data' microservice that others query or subscribe to for updates, or each service caching a local copy that's kept in sync via events when the source data changes — the choice depends on how fresh the data needs to be and how tolerant the system is of brief staleness."},
{q:"What is a common cause of cascading failures in microservices, and how is it prevented?", a:"One slow or failing service causes calling services to pile up waiting requests, exhausting their own resources (threads/connections) and becoming slow too, spreading the failure. Prevented with timeouts, Circuit Breakers, bulkheads, and graceful degradation/fallbacks."},
{q:"What is graceful degradation in a microservices system?", a:"Designing a service to still provide partial or reduced functionality when a dependency is unavailable (e.g., showing cached recommendations instead of real-time ones), rather than failing the entire request outright."},
{q:"What is the role of feature flags in a microservices deployment strategy?", a:"Letting new functionality be deployed to production but toggled on/off (or for specific user segments) without a new deployment, decoupling code deployment from feature release and enabling safer rollouts and quick rollback of a problematic feature."},
{q:"What is the difference between a microservice and a serverless function (like AWS Lambda)?", a:"A microservice is a continuously running (or auto-scaled) deployed service you manage the runtime/lifecycle for. A serverless function runs only in response to an event/trigger, with the cloud provider managing scaling and infrastructure entirely — useful for event-driven, sporadic workloads."},
{q:"How would you approach testing a system built from many microservices?", a:"A mix of unit tests per service, contract tests between service pairs to catch API incompatibilities, and a smaller number of true end-to-end tests for critical business flows — relying heavily on the lower, faster test layers since full end-to-end tests across many services are slow and brittle."},
{q:"What is the test pyramid, and how does it apply to microservices testing?", a:"A guideline suggesting many fast unit tests at the base, fewer integration/contract tests in the middle, and very few slow, brittle end-to-end tests at the top — microservices especially benefit from strong contract testing to avoid needing excessive slow end-to-end coverage."},
{q:"What is the concept of 'smart endpoints, dumb pipes' in microservices?", a:"A principle (from Martin Fowler's microservices writing) that business logic should live in the services themselves (smart endpoints), while the communication mechanism between them (the pipes — HTTP, message queues) should stay simple, rather than putting business logic into a complex central messaging/orchestration layer."},
{q:"How would you handle configuration management across many microservices?", a:"Use externalized configuration (e.g., a config server, environment variables, or a secrets manager) rather than hardcoding values into each service, so configuration changes don't require rebuilding and redeploying the service."},
{q:"What is a distributed cache, and why might microservices use one (e.g., Redis)?", a:"A cache accessible across multiple service instances (unlike an in-memory cache local to one instance), letting all instances of a service — or even multiple services — share cached data consistently, reducing repeated database load."},
{q:"What is cache invalidation, and why is it considered a hard problem in distributed systems?", a:"Ensuring cached data is updated or removed when the underlying source data changes, so services don't serve stale data. It's hard in distributed systems because multiple services/instances may cache the same data independently with no single point that knows when to invalidate all of them."},
{q:"What is the strangler pattern's relationship to legacy system migration, in your own words based on your government platform integration experience?", a:"It lets you incrementally connect new microservices to legacy systems (like a land records system or a national farmer registry) piece by piece, routing specific functionality through new services while the legacy system still handles what hasn't been migrated yet, reducing risk versus a full rewrite."},
{q:"How would you approach integrating a microservice with an external government or third-party system that has strict uptime/security requirements?", a:"Isolate the integration behind a dedicated adapter service, apply strict timeouts and Circuit Breakers since you don't control that system's reliability, encrypt data in transit, implement thorough audit logging for compliance, and design fallback/retry behavior carefully given the sensitivity of the data (e.g., biometric or identity data)."},
{q:"What is the role of an Ingress controller in a Kubernetes-based microservices deployment?", a:"Manages external HTTP/HTTPS access into the cluster, routing incoming requests to the correct internal service based on hostname or path — effectively acting as a Kubernetes-native reverse proxy/load balancer."},
{q:"What is the difference between a ConfigMap and a Secret in Kubernetes?", a:"A ConfigMap stores non-sensitive configuration data as key-value pairs. A Secret stores sensitive data (like passwords or API keys) similarly, but with additional handling intended to reduce accidental exposure (like base64 encoding and restricted access), though it should still be paired with proper encryption at rest."},
{q:"What is horizontal pod autoscaling in Kubernetes?", a:"Automatically increasing or decreasing the number of running pod instances of a service based on observed metrics like CPU or memory usage, so the system scales to match actual load without manual intervention."},
{q:"What is the difference between a rolling update and a recreate deployment strategy in Kubernetes?", a:"Rolling update gradually replaces old pods with new ones, keeping the service available throughout. Recreate deployment stops all old pods before starting new ones, causing downtime but ensuring no two versions run simultaneously — useful when versions can't coexist safely."},
{q:"How would you handle a data migration when a microservice's database schema needs to change, without downtime?", a:"Use backward-compatible, incremental migrations (e.g., add new columns without removing old ones immediately, dual-write during transition), deploy code that can handle both old and new schema during the transition window, then clean up the old schema only after the new code is fully rolled out."},
{q:"What is the difference between coupling and cohesion, and why do they matter for microservices design?", a:"Cohesion measures how closely related the responsibilities within one service are — high cohesion is good. Coupling measures how much one service depends on the internal details of another — low coupling is good. Well-designed microservices aim for high cohesion within a service and low coupling between services."},
{q:"What is an anti-pattern called a 'distributed monolith,' and how does it happen?", a:"When services are physically deployed separately but are so tightly coupled (shared databases, synchronous chains of calls, coordinated deployments) that you get all the complexity of a distributed system with none of the independent-deployment benefits of true microservices."},
{q:"How would you avoid creating a distributed monolith when splitting services?", a:"Give each service its own database, minimize synchronous chains of dependent calls between services, design around business capabilities rather than technical layers, and ensure each service can be deployed independently without requiring coordinated releases of other services."},
{q:"What is the role of API documentation and a shared contract registry in a microservices team?", a:"Ensures teams that don't own a given service can understand how to call it correctly without reading its source code, and a shared registry of contracts (e.g., OpenAPI specs) helps catch breaking changes and keeps cross-team integration predictable."},
{q:"What is a common way to handle authentication once at the gateway vs re-authenticating at every internal service?", a:"The gateway commonly validates the initial user token once and then passes a lighter-weight internal token or trusted header downstream (often over mTLS within a service mesh), so internal services trust the gateway rather than each independently validating the original credentials."},
{q:"What would you monitor to detect that a Circuit Breaker has opened in production, and why does that matter operationally?", a:"Metrics/alerts on circuit state changes (closed to open) and the associated error rate spike — this matters because an open circuit means real functionality is currently degraded for users, and the team needs to investigate the downstream service's failure quickly, not just rely on the circuit silently protecting the system."},
{q:"What is the difference between a stateless service and a stateful service, and can microservices ever be stateful?", a:"Stateless services keep no client-specific data between requests. Some microservices are intentionally stateful (e.g., a service managing an in-memory session or a real-time connection), but this requires extra care for scaling and failover — stateless design remains the default preference wherever possible."},
{q:"What is the purpose of API request/response schema validation at the gateway or service boundary?", a:"Catches malformed or unexpected input early, before it reaches business logic, protecting downstream services from bad data and providing clear, immediate error feedback to the calling client."},
{q:"How would you approach rolling back a bad microservice deployment quickly in production?", a:"Favor deployment strategies (blue-green, canary) that make rollback fast — like switching traffic back to the previous stable version — combined with good monitoring/alerting to detect the problem quickly, and keeping deployments small/frequent so each rollback affects a limited change set."},
{q:"What is the significance of designing APIs to be backward compatible in a microservices system with independent deployments?", a:"Since services deploy independently and you can't guarantee every consumer upgrades at the same time, a backward-incompatible change to one service's API can break other services that haven't been updated yet — backward compatibility avoids forcing synchronized, risky coordinated deployments."},
{q:"What is a common approach for handling long-running business processes that span multiple microservices (e.g., an order fulfillment process)?", a:"Model it as a Saga — a sequence of steps across services, each with its own local transaction and compensating action, often coordinated either by an orchestrator service or via choreographed events, rather than trying to force it into a single distributed transaction."},
{q:"How would you explain the Circuit Breaker implementation you built (from your CV) in interview terms?", a:"Describe it concretely: which downstream call it protected, what failure threshold triggered the circuit to open, what happened for callers while it was open (fast failure or fallback response), and how it recovered — tie it back to the real business impact, like preventing one struggling dependency from taking down transaction processing."},
{q:"What questions would you expect an interviewer to ask if you mention 'high-availability microservices' on your resume?", a:"Expect: how you handled failover, what your actual uptime numbers were and how you measured them, how you tested for resilience, what happened when a dependency failed, and how the system scaled under peak load — be ready with specific, concrete answers rather than generic descriptions."}
];
