const angularData = [
{q:"What is Angular?", a:"A TypeScript-based frontend framework (by Google) for building single-page applications, providing a complete toolkit — components, routing, forms, HTTP client, and dependency injection — out of the box."},
{q:"What is the difference between Angular and AngularJS?", a:"AngularJS (1.x) was JavaScript-based and used a different architecture (controllers, $scope). Angular (2+) is a complete rewrite in TypeScript, based on components, and is significantly faster and more structured — they are not simply different versions of the same codebase."},
{q:"What is a component in Angular?", a:"A TypeScript class decorated with @Component that controls a piece of the UI — combining a template (HTML), styles (CSS), and logic (the class) into one reusable unit.", ex:"@Component({ selector: 'app-user-card', templateUrl: './user-card.html' }) export class UserCardComponent { }", code:true},
{q:"What is a module in Angular (NgModule)?", a:"A container that groups related components, directives, pipes, and services together, declaring what belongs to a feature area and what dependencies it needs — though newer Angular versions increasingly favor standalone components over NgModules."},
{q:"What are standalone components, and how do they change Angular's module system?", a:"Introduced to simplify Angular, standalone components declare their own dependencies directly (via the 'imports' array in the @Component decorator) without needing to belong to an NgModule, reducing boilerplate for smaller or simpler applications."},
{q:"What is data binding in Angular?", a:"A mechanism to synchronize data between a component's TypeScript class and its HTML template, so changes in one are reflected in the other automatically."},
{q:"What is the difference between interpolation and property binding?", a:"Interpolation ({{ value }}) inserts a value as text inside HTML content. Property binding ([property]=\"value\") binds a value to a specific DOM element property, useful for non-string values like booleans or objects.", ex:"<h1>{{ title }}</h1>\n<img [src]=\"imageUrl\">", code:true},
{q:"What is event binding in Angular?", a:"Binding a DOM event (like a click) to a method in the component class using parentheses syntax.", ex:"<button (click)=\"saveOrder()\">Save</button>", code:true},
{q:"What is two-way data binding, and how is it implemented in Angular?", a:"Binding that keeps a component property and a form input in sync in both directions — implemented with the banana-in-a-box syntax [(ngModel)], combining property binding and event binding into one.", ex:"<input [(ngModel)]=\"username\">", code:true},
{q:"What are directives in Angular?", a:"Instructions that Angular applies to DOM elements to change their appearance or behavior — categorized as component directives (with a template), structural directives (that change DOM layout, like *ngIf), and attribute directives (that change appearance/behavior, like ngClass)."},
{q:"What is the difference between *ngIf and [hidden]?", a:"*ngIf completely adds or removes the element from the DOM based on a condition. [hidden] keeps the element in the DOM but toggles its CSS display property — *ngIf is usually preferred for elements with heavier content or that shouldn't exist in the DOM when false."},
{q:"What is *ngFor used for?", a:"A structural directive that repeats a template once for each item in a collection, commonly used to render lists.", ex:"<li *ngFor=\"let user of users\">{{ user.name }}</li>", code:true},
{q:"What is trackBy in *ngFor, and why is it important for performance?", a:"A function that tells Angular how to uniquely identify each item in a list, so when the list updates, Angular only re-renders items that actually changed instead of destroying and recreating every DOM element in the list.", ex:"<li *ngFor=\"let user of users; trackBy: trackByUserId\">{{ user.name }}</li>", code:true},
{q:"What is the difference between ngClass and ngStyle?", a:"ngClass conditionally applies one or more CSS classes to an element. ngStyle conditionally applies inline CSS style properties directly, without needing a predefined class."},
{q:"What is a service in Angular?", a:"A class focused on a specific piece of reusable logic (like fetching data from an API) that can be injected into components via dependency injection, keeping components focused on the UI rather than business/data logic."},
{q:"What is dependency injection in Angular?", a:"A design pattern where Angular's injector provides a component or service with the dependencies it needs (like another service), rather than the class creating those dependencies itself — enabling loose coupling and easier testing.", ex:"constructor(private userService: UserService) { }", code:true},
{q:"What does @Injectable() do?", a:"Marks a class as available to be injected as a dependency and lets Angular's DI system know how to construct it, including any of its own dependencies."},
{q:"What is the difference between providedIn: 'root' and providing a service in a specific module/component?", a:"providedIn: 'root' registers the service as a singleton shared across the whole application. Providing it at a component or module level instead creates a separate instance scoped to just that part of the app — useful when you want isolated instances per feature."},
{q:"What is Angular's component lifecycle, and what are the main lifecycle hooks?", a:"A sequence of phases a component goes through: ngOnChanges (input properties changed), ngOnInit (component initialized, called once), ngDoCheck (custom change detection), ngAfterViewInit (view fully initialized), and ngOnDestroy (cleanup before the component is removed)."},
{q:"What is ngOnInit used for, and why not just use the constructor?", a:"ngOnInit runs after Angular has set up the component's input bindings, making it the right place for initialization logic that depends on those inputs. The constructor should be reserved mainly for dependency injection, not business logic that needs inputs to already be resolved."},
{q:"What is ngOnDestroy used for?", a:"Cleanup logic that runs just before a component is destroyed — commonly used to unsubscribe from Observables or clear timers/intervals to prevent memory leaks."},
{q:"What is @Input() used for?", a:"Declares a property that a parent component can pass data into a child component through, via property binding in the parent's template.", ex:"@Input() userName: string;\n// parent template: <app-user [userName]=\"currentUser.name\"></app-user>", code:true},
{q:"What is @Output() used for?", a:"Declares a custom event a child component can emit, letting a parent component listen and react to something that happened inside the child, typically paired with an EventEmitter.", ex:"@Output() saved = new EventEmitter<Order>();\nthis.saved.emit(order);", code:true},
{q:"What is the difference between @Input()/@Output() and a shared service for component communication?", a:"@Input()/@Output() work well for direct parent-child communication. A shared service (often with an Observable/Subject) is better for communication between components that aren't directly related, like siblings or distant components in the tree."},
{q:"What is Angular's change detection mechanism, at a high level?", a:"Angular tracks when data might have changed (from events, HTTP responses, timers) and re-checks the component tree to update the DOM wherever bound values changed, using a mechanism historically built on zone.js to detect async operations completing."},
{q:"What is the difference between the Default and OnPush change detection strategies?", a:"Default checks every component on every change detection cycle, regardless of whether its inputs actually changed. OnPush only re-checks a component when its @Input() references change (not just mutate), an event fires within it, or an Observable it's using emits — significantly improving performance for large applications."},
{q:"What is zone.js, and what problem does it solve for Angular?", a:"A library that patches asynchronous browser APIs (setTimeout, HTTP calls, DOM events) so Angular can automatically detect when something might have changed and trigger change detection, without the developer needing to manually tell Angular to re-check the view."},
{q:"What is Angular Signals, and how do they relate to change detection?", a:"A newer reactive primitive in Angular that tracks dependencies explicitly and updates only the parts of the UI that depend on a changed signal value, offering a more fine-grained and often more predictable alternative to zone.js-based change detection.", ex:"count = signal(0); increment() { this.count.update(v => v + 1); }", code:true},
{q:"What is RxJS, and why does Angular rely on it heavily?", a:"A library for reactive programming using Observables — streams of values over time. Angular uses it extensively for handling asynchronous operations like HTTP requests, user input events, and route parameter changes in a consistent, composable way."},
{q:"What is an Observable in RxJS?", a:"A representation of a stream of values that can be emitted over time, which you subscribe to in order to react as new values arrive — unlike a Promise, an Observable can emit multiple values and can be cancelled."},
{q:"What is the difference between an Observable and a Promise?", a:"A Promise resolves once with a single value and can't be cancelled. An Observable can emit multiple values over time, supports powerful operators (map, filter, debounce), and can be unsubscribed/cancelled if no longer needed."},
{q:"What does subscribing to an Observable actually do?", a:"It executes the Observable's logic and registers a callback (or set of callbacks for next/error/complete) to run each time the Observable emits a value, errors, or completes."},
{q:"Why is it important to unsubscribe from Observables in Angular components?", a:"If a subscription isn't cleaned up when a component is destroyed, the callback can keep running and holding a reference to the destroyed component, causing a memory leak — commonly handled in ngOnDestroy or with the async pipe, which unsubscribes automatically."},
{q:"What is the async pipe, and why is it useful?", a:"A template pipe that automatically subscribes to an Observable (or Promise) and automatically unsubscribes when the component is destroyed, eliminating the need for manual subscription management in many cases.", ex:"<div>{{ user$ | async }}</div>", code:true},
{q:"What is the difference between map(), filter(), and switchMap() in RxJS?", a:"map() transforms each emitted value. filter() only lets values matching a condition through. switchMap() maps each emitted value to a new inner Observable, automatically cancelling the previous inner Observable if a new value arrives before it completes — commonly used for search-as-you-type to cancel stale requests."},
{q:"When would you use switchMap vs mergeMap vs concatMap?", a:"switchMap cancels the previous inner Observable when a new value arrives (good for search/autocomplete where only the latest result matters). mergeMap runs all inner Observables concurrently without cancelling (good for independent parallel requests). concatMap runs inner Observables one at a time in order (good when order and sequencing matter)."},
{q:"What is a Subject in RxJS, and how is it different from a regular Observable?", a:"A Subject is both an Observable and an Observer — it can be subscribed to like an Observable, but you can also manually push values into it with .next(), making it useful for multicasting events (like a custom event bus) rather than a value stream defined internally."},
{q:"What is the difference between a Subject and a BehaviorSubject?", a:"A Subject has no initial value and only emits to subscribers that subscribed after a value was pushed. A BehaviorSubject requires an initial value and always immediately emits its current/latest value to any new subscriber, which is useful for representing current state."},
{q:"How does Angular's HttpClient work, and what does it return?", a:"Angular's built-in service for making HTTP requests, which returns Observables for each request — letting you use RxJS operators to transform, retry, or combine the response as needed.", ex:"this.http.get<User[]>('/api/users').subscribe(users => this.users = users);", code:true},
{q:"How do you handle errors from an HTTP request in Angular?", a:"Using the catchError RxJS operator within a pipe, or handling the error callback in subscribe(), often combined with an HttpInterceptor to handle common errors (like 401 Unauthorized) globally across all requests."},
{q:"What is an HttpInterceptor, and what is it commonly used for?", a:"A mechanism to intercept every outgoing HTTP request and/or incoming response, commonly used to automatically attach an authentication token to requests, handle global error responses, or add logging.", ex:"intercept(req, next) { const cloned = req.clone({ setHeaders: { Authorization: `Bearer ${token}` } }); return next.handle(cloned); }", code:true},
{q:"How would you attach a JWT token to every outgoing API request in an Angular app connecting to a Spring Boot backend?", a:"Implement an HttpInterceptor that clones each outgoing request, adds an Authorization: Bearer <token> header (reading the token from a service or storage), and passes the modified request along — this avoids manually adding the header in every individual service call."},
{q:"What is Angular Router used for?", a:"Angular's built-in module for managing navigation between different views/components in a single-page application without a full page reload, based on URL paths."},
{q:"What is a route guard, and what types exist?", a:"A mechanism to control whether navigation to (or away from) a route is allowed — common guards include CanActivate (controls entering a route, e.g., checking authentication), CanDeactivate (controls leaving, e.g., warning about unsaved changes), and Resolve (pre-fetches data before the route activates)."},
{q:"How would you protect a route so only authenticated users can access it?", a:"Implement a CanActivate guard that checks whether the user is authenticated (e.g., a valid token exists), returning true to allow navigation or redirecting to a login page and returning false if not.", ex:"canActivate(): boolean { if (this.auth.isLoggedIn()) return true; this.router.navigate(['/login']); return false; }", code:true},
{q:"What is lazy loading in Angular routing, and why does it matter for performance?", a:"Loading a feature module's JavaScript code only when the user navigates to a route that needs it, instead of bundling everything into the initial page load — significantly reducing the app's initial load time for larger applications."},
{q:"What is the difference between a route parameter and a query parameter in Angular routing?", a:"A route parameter is part of the URL path itself (e.g., /users/:id), typically identifying a specific resource. A query parameter is appended after a '?' (e.g., /users?sort=name), typically used for optional filters or settings that don't change what resource is being viewed."},
{q:"What are Angular Reactive Forms, and how do they differ from Template-Driven Forms?", a:"Reactive Forms build the form's structure and validation explicitly in the component's TypeScript class using FormGroup/FormControl, giving more control and easier testability. Template-Driven Forms build the structure mostly in the HTML template using ngModel, which is simpler for very basic forms but harder to manage for complex validation logic."},
{q:"What is a FormControl in Angular Reactive Forms?", a:"Represents a single form input's value and validation state, tracked independently of the DOM, so you can read/set its value and check validity directly from the component class.", ex:"email = new FormControl('', [Validators.required, Validators.email]);", code:true},
{q:"What is a FormGroup in Angular Reactive Forms?", a:"A collection of related FormControls (and/or nested FormGroups) grouped together, representing a whole form or a logical section of it, with aggregate validity based on all its controls.", ex:"form = new FormGroup({ name: new FormControl(''), email: new FormControl('') });", code:true},
{q:"What is a FormArray in Angular Reactive Forms?", a:"A dynamic, resizable list of FormControls or FormGroups, used when a form needs a variable number of repeated fields, like adding multiple phone numbers or line items."},
{q:"How do you display a validation error message for a specific field in a Reactive Form?", a:"Check the control's status (e.g., form.get('email').invalid) combined with whether it's been touched or dirty (so errors don't show immediately on page load), and conditionally render the appropriate message in the template.", ex:"<div *ngIf=\"form.get('email').invalid && form.get('email').touched\">Invalid email</div>", code:true},
{q:"What is a custom validator in Angular forms?", a:"A function you write that checks a FormControl's value against custom business rules (beyond built-in validators like required or email) and returns an error object if invalid, or null if valid.", ex:"function forbiddenNameValidator(control: AbstractControl): ValidationErrors | null { return control.value === 'admin' ? { forbiddenName: true } : null; }", code:true},
{q:"What is content projection in Angular (using ng-content)?", a:"A way for a parent component to pass HTML content into a child component's template at a specific insertion point, letting the child component wrap or style content it doesn't control the content of — similar to 'slots' in other frameworks.", ex:"<!-- child template --> <div class=\"card\"><ng-content></ng-content></div>", code:true},
{q:"What is a pipe in Angular?", a:"A simple function used in templates to transform displayed data, like formatting a date or currency value, without changing the underlying data itself.", ex:"<p>{{ price | currency:'USD' }}</p>\n<p>{{ birthDate | date:'mediumDate' }}</p>", code:true},
{q:"What is the difference between a pure pipe and an impure pipe?", a:"A pure pipe (the default) only re-runs when its input reference changes, which is efficient. An impure pipe re-runs on every change detection cycle regardless, which can hurt performance if overused, but is sometimes necessary for pipes depending on mutable data or external state."},
{q:"How do you create a custom pipe in Angular?", a:"Implement the PipeTransform interface with a transform() method, and decorate the class with @Pipe({ name: 'yourPipeName' }).", ex:"@Pipe({ name: 'truncate' }) export class TruncatePipe implements PipeTransform { transform(value: string, limit = 20) { return value.length > limit ? value.slice(0, limit) + '...' : value; } }", code:true},
{q:"What is Angular's ViewChild decorator used for?", a:"Gives a component's TypeScript class direct access to a child component, directive, or DOM element referenced in its template, useful for calling methods on a child or reading its properties directly.", ex:"@ViewChild('nameInput') nameInput: ElementRef;", code:true},
{q:"What is the difference between ViewChild and ContentChild?", a:"ViewChild accesses elements defined in the component's own template. ContentChild accesses elements that were projected into the component from outside via ng-content, i.e., content the parent placed inside this component's tags."},
{q:"What is Angular CLI, and what does it help with?", a:"A command-line tool for generating Angular projects, components, services, and modules with consistent boilerplate, plus running the dev server, tests, and production builds — significantly speeding up common development tasks."},
{q:"What is Ahead-of-Time (AOT) compilation in Angular, and how does it differ from Just-in-Time (JIT)?", a:"AOT compiles Angular templates into efficient JavaScript during the build process (before the app runs in the browser), resulting in faster startup and smaller bundles. JIT compiles templates in the browser at runtime, which is slower to start but was historically used for faster development iteration — AOT is standard for production builds."},
{q:"What is tree shaking, and why does it matter for an Angular app's bundle size?", a:"A build optimization that removes unused code from the final JavaScript bundle, reducing file size and improving load time — Angular's build tooling performs this automatically as part of a production build."},
{q:"What is the purpose of environment files (environment.ts, environment.prod.ts) in Angular?", a:"Store environment-specific configuration (like API base URLs) that's swapped automatically at build time depending on whether you're building for development or production, avoiding hardcoded values that differ per environment."},
{q:"How would you configure CORS-related issues when an Angular app calls a Spring Boot backend during local development?", a:"Either configure the Spring Boot backend to explicitly allow the Angular dev server's origin (e.g., http://localhost:4200) via CORS configuration, or use Angular CLI's proxy configuration (proxy.conf.json) to route API calls through the dev server, avoiding cross-origin requests during development entirely."},
{q:"What is a singleton service in Angular, and how do you ensure a service is a singleton?", a:"A service with only one shared instance across the entire application. Using @Injectable({ providedIn: 'root' }) ensures Angular creates and shares a single instance application-wide, rather than creating separate instances per module or component."},
{q:"What is state management in a frontend application, and when might you need a dedicated library (like NgRx) beyond services?", a:"Managing shared application data (like the current user, cart contents, or UI state) consistently across components. Simple apps can manage this with shared services and Subjects/Signals; larger apps with complex, deeply shared state across many unrelated components often benefit from a dedicated state management library like NgRx for predictable, centralized state updates."},
{q:"What is NgRx, at a high level?", a:"A state management library for Angular based on the Redux pattern — application state lives in a single centralized store, changes happen through dispatched actions handled by pure reducer functions, and components read state via selectors, making state changes predictable and traceable."},
{q:"What are the core building blocks of NgRx (Store, Actions, Reducers, Selectors, Effects)?", a:"Store holds the single application state tree. Actions describe what happened (events). Reducers are pure functions that compute new state based on the current state and an action. Selectors read specific slices of state efficiently. Effects handle side effects (like API calls) triggered by actions, outside of the pure reducers."},
{q:"What is the difference between a template-driven approach to forms validation and displaying server-side validation errors from a Spring Boot API?", a:"Client-side (template-driven or reactive) validation catches obvious errors immediately in the browser for a responsive UX. Server-side errors (e.g., a uniqueness constraint violation only the database can catch) need to be caught from the HTTP error response and mapped back onto the relevant form field's error state after the request completes."},
{q:"What is Angular Universal, and what problem does it solve?", a:"A technology for server-side rendering (SSR) of Angular applications, rendering the initial HTML on the server before sending it to the browser — improving perceived load time and enabling better SEO for public-facing pages, since search engine crawlers see fully rendered content immediately."},
{q:"What is the difference between client-side rendering and server-side rendering, in the context of Angular apps?", a:"Client-side rendering sends a mostly-empty HTML shell and lets the browser download and run JavaScript to build the page, which can be slower to show initial content. Server-side rendering (Angular Universal) renders the initial page HTML on the server, so the browser has visible content immediately while JavaScript loads in the background to make it interactive."},
{q:"What is a resolver in Angular routing, and when would you use one?", a:"A service that pre-fetches data before a route is activated, so the component doesn't render in an empty/loading state — useful when you want the target page to have its data ready the moment it appears, rather than showing a spinner after navigation."},
{q:"What is the purpose of Angular's HttpClientTestingModule in unit tests?", a:"Lets you mock HTTP requests made through HttpClient in tests, asserting what requests were made and controlling exactly what response is returned, without needing a real backend server running during tests."},
{q:"How would you unit test an Angular component that depends on a service?", a:"Use Angular's TestBed to configure a testing module, providing a mock/stub version of the dependent service (or a spy) instead of the real one, then create the component and assert on its behavior in isolation from the real service's implementation.", ex:"TestBed.configureTestingModule({ declarations: [UserCardComponent], providers: [{ provide: UserService, useValue: mockUserService }] });", code:true},
{q:"What is the purpose of Jasmine and Karma (or Jest) in Angular testing?", a:"Jasmine is the testing framework providing the syntax for writing tests (describe, it, expect). Karma (the traditional default) or Jest (an increasingly common alternative) is the test runner that actually executes those tests, typically in a real or headless browser environment."},
{q:"What is a spy in Angular/Jasmine testing?", a:"A test double that replaces a real function or method, letting you verify it was called (and with what arguments) without actually executing its real implementation, and optionally control what it returns."},
{q:"What is the difference between a component and a directive in Angular?", a:"A component is technically a directive with its own template. A plain directive (attribute or structural) doesn't have its own template — it modifies the appearance or behavior of an existing DOM element or component instead."},
{q:"How would you create a custom attribute directive in Angular?", a:"Implement a class decorated with @Directive({ selector: '[yourAttribute]' }), injecting ElementRef and/or Renderer2 to manipulate the host element's appearance or behavior directly.", ex:"@Directive({ selector: '[highlight]' }) export class HighlightDirective { constructor(el: ElementRef) { el.nativeElement.style.backgroundColor = 'yellow'; } }", code:true},
{q:"Why is Renderer2 generally preferred over directly manipulating nativeElement in Angular directives?", a:"Renderer2 abstracts DOM manipulation so it works correctly across different rendering environments (like server-side rendering with Angular Universal, where there's no real browser DOM), whereas direct nativeElement manipulation assumes a browser DOM is always present."},
{q:"What is the purpose of Angular's ElementRef, and why should it be used cautiously?", a:"Gives direct access to the underlying native DOM element. It should be used cautiously because directly manipulating the DOM through it bypasses Angular's change detection and rendering abstractions, and can break in server-side rendering contexts."},
{q:"What is the difference between a Smart (Container) component and a Dumb (Presentational) component pattern in Angular?", a:"A smart component handles data fetching, business logic, and state, passing data down to dumb components. A dumb component just receives data via @Input() and emits events via @Output(), focused purely on presentation — this separation improves reusability and testability."},
{q:"How would you optimize an Angular application experiencing slow rendering performance with a large list of items?", a:"Use OnPush change detection, add trackBy to *ngFor loops, consider virtual scrolling (rendering only visible items in a long list, via Angular CDK's virtual scroll), and avoid heavy computation directly in templates (move it to memoized values in the component instead)."},
{q:"What is Angular CDK's virtual scrolling, and why does it improve performance for large lists?", a:"A technique that only renders the DOM elements currently visible in the viewport (plus a small buffer), instead of rendering every item in a potentially huge list — dramatically reducing the number of DOM nodes and improving scroll performance."},
{q:"What is debouncing, and how would you implement it for a live search input in Angular using RxJS?", a:"Delaying processing of rapid, repeated events (like every keystroke) until there's a pause, to avoid firing excessive API calls — implemented using the RxJS debounceTime operator on an Observable created from the input's value changes.", ex:"this.searchControl.valueChanges.pipe(debounceTime(300), distinctUntilChanged(), switchMap(term => this.api.search(term))).subscribe(...)", code:true},
{q:"What is distinctUntilChanged() used for in RxJS, and why pair it with debounceTime for search inputs?", a:"Prevents emitting a value if it's identical to the previous one, avoiding unnecessary duplicate API calls — commonly paired with debounceTime so a search only fires after the user pauses typing AND the value has actually changed."},
{q:"What is the purpose of Angular's HttpClient interceptors chain, and can you have multiple interceptors?", a:"Yes — multiple interceptors can be registered and they execute in the order they're provided, each able to modify the outgoing request or incoming response before passing it to the next interceptor in the chain, useful for layering concerns like auth, logging, and error handling separately."},
{q:"How would you handle displaying a loading spinner globally while any HTTP request is in progress?", a:"Implement an HttpInterceptor that increments a loading counter (or sets a loading flag via a shared service) when a request starts and decrements/clears it when the request completes (in both success and error cases), with a component subscribed to that state to show/hide a spinner."},
{q:"What is Angular's approach to lazy-loaded feature modules improving initial bundle size, in practice?", a:"Feature areas not needed immediately (like an admin dashboard most users never visit) are split into separate JavaScript chunks loaded only when the corresponding route is navigated to, keeping the main initial bundle smaller and the first page load faster."},
{q:"What is the difference between Angular Material and Angular CDK?", a:"Angular Material is a full pre-styled component library (buttons, dialogs, tables) following Material Design. Angular CDK (Component Dev Kit) provides the underlying behavioral building blocks (overlay positioning, virtual scrolling, drag-and-drop) without imposing a specific visual style, letting you build custom-styled components on top."},
{q:"What is the purpose of Angular's providers array at different levels (root, module, component)?", a:"Determines the scope/lifetime of a service instance — providing at root creates one app-wide singleton, while providing at a module or component level creates a new instance scoped to just that module's or component's (and its children's) injector."},
{q:"How would you share data between two sibling components that don't have a direct parent-child relationship?", a:"Use a shared service (often with a BehaviorSubject or Signal) injected into both components — one component updates the shared state through the service, and the other reacts to those changes via subscription or the async pipe."},
{q:"What is Angular's strict mode, and why might you enable it for a new project?", a:"A set of stricter TypeScript and Angular compiler checks (like strict null checks) enabled via the CLI, catching more potential bugs at compile time at the cost of needing more careful, explicit typing throughout the codebase."},
{q:"What is the significance of typing HTTP responses with interfaces/models in an Angular service calling a Spring Boot API?", a:"Gives compile-time type safety and autocomplete when working with API response data, catching mismatches between what the frontend expects and what the backend actually returns earlier, during development rather than at runtime.", ex:"interface User { id: number; name: string; email: string; }\nthis.http.get<User[]>('/api/users')", code:true},
{q:"How would you structure an Angular project for a mid-sized application with several distinct feature areas?", a:"Organize by feature module (e.g., orders, users, dashboard), each with its own components, services, and routing, plus a shared/core module for cross-cutting concerns like the HTTP interceptor, auth guard, and common UI components used across features."},
{q:"What is the difference between a 'core' module and a 'shared' module in a typical Angular project structure?", a:"A core module holds singleton services and app-wide concerns meant to be imported only once, in the root module (e.g., auth service, interceptors). A shared module holds reusable components/pipes/directives with no singleton state, meant to be imported by multiple feature modules."},
{q:"What is the purpose of using interfaces for form models in Angular Reactive Forms?", a:"Provides compile-time type checking on form values, catching typos in property names and mismatches between the form structure and how the data is used elsewhere in the component, reducing runtime bugs."},
{q:"How would you handle a scenario where an Angular form needs to submit data to a Spring Boot endpoint that returns validation errors per field?", a:"Catch the HTTP error response (typically a structured JSON body with field-level messages), then loop through the returned errors and call setErrors() on the corresponding FormControl for each field, so the UI highlights exactly which fields the backend rejected and why."},
{q:"What is the purpose of using RxJS's forkJoin operator?", a:"Runs multiple Observables in parallel and waits for all of them to complete, emitting a single combined result once every one has finished — useful when a component needs data from several independent API calls before it can render.", ex:"forkJoin({ user: this.userApi.get(id), orders: this.orderApi.getByUser(id) }).subscribe(({ user, orders }) => { ... });", code:true},
{q:"What is Angular's Router's preloading strategy, and why might you enable it?", a:"A configuration controlling whether lazy-loaded modules are fetched in the background after the initial app loads (even before the user navigates to them), balancing fast initial load with faster subsequent navigation once background preloading completes."},
{q:"What is the difference between routerLink and a plain HTML anchor href in Angular?", a:"routerLink lets Angular's router handle navigation internally without a full page reload, preserving the single-page application experience. A plain href would cause the browser to do a full page reload, losing the in-memory application state."},
{q:"How would you implement role-based UI visibility in Angular (e.g., hiding an admin button for non-admin users), working with a Spring Boot backend that issues roles in a JWT?", a:"Decode the JWT (or use data returned from a /me endpoint) to determine the user's roles after login, store them in an auth service, and use *ngIf checks against that service's role data in templates to conditionally show or hide UI elements — while still enforcing the actual authorization server-side, since client-side hiding alone isn't real security."},
{q:"What is the significance of never relying solely on frontend route guards or UI hiding for security?", a:"Frontend checks (route guards, *ngIf visibility) are purely for user experience — they can be bypassed by anyone inspecting or modifying client-side code. Real authorization must always be enforced on the backend (e.g., Spring Security), since the frontend can't be trusted as the actual security boundary."},
{q:"What is the purpose of a barrel file (index.ts) in an Angular project, and what's a downside of overusing it?", a:"Re-exports multiple modules from a single file for cleaner imports elsewhere. Overusing barrel files across a large app can sometimes hurt build performance and tree-shaking effectiveness, and can create circular dependency issues if not organized carefully."},
{q:"What is the difference between Angular's HttpClient.get() returning a typed Observable vs. using 'any' as the type?", a:"Using a specific type (or interface) gives compile-time safety and IDE autocomplete for the response shape. Using 'any' bypasses type checking entirely, which is faster to write but loses the safety net that catches mismatches between expected and actual API response shapes."},
{q:"How would you approach migrating an Angular application from NgModules to standalone components incrementally?", a:"Convert components one at a time to standalone (adding standalone: true and their own imports array), starting with leaf components with few dependencies, while leaving the rest of the app on NgModules during the transition, since Angular supports both approaches coexisting."},
{q:"What questions might an interviewer ask about how you connected your Angular dashboards to Spring Boot REST APIs (from your CV)?", a:"Expect questions about how you handled authentication tokens on requests (interceptors), how you managed loading and error states in the UI, how you structured services to call the backend, and how you handled real-time or frequently-changing data (polling vs. websockets) for your workflow automation modules — be ready with specific implementation details, not just high-level descriptions."}
];
