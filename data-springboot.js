const springbootData = [
{q:"What is the Spring Framework?", a:"A framework that manages object creation and wiring (Dependency Injection) for Java applications, keeping classes loosely coupled and easier to test."},
{q:"What is Spring Boot, and how is it different from Spring?", a:"Spring Boot is built on Spring and removes most manual setup — it provides auto-configuration, an embedded server, and starter dependencies so you get a working app with minimal boilerplate, unlike classic Spring's manual XML/Java config."},
{q:"What is Dependency Injection (DI)?", a:"Instead of a class creating its own dependencies with 'new', Spring creates and injects them for you, usually via the constructor — keeping classes loosely coupled and easy to test with mocks.", ex:"@Service class OrderService { private final PaymentGateway gw; OrderService(PaymentGateway gw){ this.gw = gw; } }", code:true},
{q:"What is Inversion of Control (IoC)?", a:"The principle behind DI — instead of your code controlling object creation, that control is handed to a framework (Spring's IoC container), which creates and manages objects for you."},
{q:"What is a Spring Bean?", a:"Any object created, configured, and managed by the Spring IoC container instead of being manually instantiated with 'new'."},
{q:"What is the Spring IoC container?", a:"The core Spring component responsible for creating, wiring, configuring, and managing the full lifecycle of all beans in an application."},
{q:"What are the types of Dependency Injection in Spring?", a:"Constructor injection (recommended — dependencies passed via constructor), setter injection (via setter methods), and field injection (@Autowired directly on a field, discouraged for required dependencies)."},
{q:"Why is constructor injection preferred over field injection?", a:"It makes dependencies explicit and mandatory (the object can't be created without them), it's far easier to unit test by passing mocks into the constructor, and it allows fields to be declared final for immutability."},
{q:"What does @Autowired do?", a:"Tells Spring to automatically inject a matching bean into a field, constructor, or setter."},
{q:"What is the difference between @Component, @Service, @Repository, and @Controller?", a:"All mark a class as a Spring-managed bean. @Component is generic. @Service marks business logic classes. @Repository marks data-access classes and translates database exceptions into Spring's unified exception hierarchy. @Controller/@RestController marks web layer classes handling HTTP requests."},
{q:"What is the difference between @Controller and @RestController?", a:"@RestController equals @Controller plus @ResponseBody — it automatically serializes return values to JSON/XML in the response body, instead of resolving to a view template."},
{q:"What does @Bean do?", a:"Used inside a @Configuration class to manually declare a bean — typically for custom setup logic or wiring in a third-party class you can't directly annotate."},
{q:"What is the default scope of a Spring bean?", a:"Singleton — only one instance is created per Spring container and shared everywhere it's injected."},
{q:"What are the different Spring bean scopes?", a:"Singleton (one shared instance, default), Prototype (new instance every request for the bean), Request (one instance per HTTP request), and Session (one instance per HTTP session)."},
{q:"What is the Spring Bean lifecycle?", a:"Spring instantiates the bean, injects its dependencies, calls any @PostConstruct method, the bean is ready for use, and on shutdown calls @PreDestroy before removing it."},
{q:"What do @PostConstruct and @PreDestroy do?", a:"@PostConstruct runs right after bean creation and dependency injection, good for initialization logic. @PreDestroy runs just before the bean is destroyed, good for cleanup like closing connections."},
{q:"What is auto-configuration in Spring Boot?", a:"Spring Boot inspects what's on the classpath (e.g., a database driver) and automatically configures relevant beans, instead of requiring you to manually configure everything."},
{q:"What is @SpringBootApplication?", a:"A combined annotation bundling @Configuration, @EnableAutoConfiguration, and @ComponentScan — placed on the main class to bootstrap a Spring Boot app."},
{q:"What is application.properties/application.yml used for?", a:"External configuration files where you set things like database URLs, server ports, and custom settings without hardcoding them into Java code.", ex:"server.port=8081\nspring.datasource.url=jdbc:postgresql://localhost:5432/mydb", code:true},
{q:"What are Spring profiles?", a:"A way to maintain different configuration sets for different environments (dev, test, prod), e.g. application-dev.yml vs application-prod.yml, switched via spring.profiles.active."},
{q:"What is REST?", a:"An architectural style for building web APIs using standard HTTP methods (GET, POST, PUT, DELETE) to operate on resources identified by URLs, typically exchanging JSON."},
{q:"Difference between PUT and PATCH?", a:"PUT replaces the entire resource with the data sent. PATCH updates only the specific fields provided, leaving the rest unchanged."},
{q:"What is @RequestMapping and its shortcuts?", a:"@RequestMapping maps HTTP requests to controller methods. Shortcuts like @GetMapping, @PostMapping, @PutMapping, and @DeleteMapping are cleaner versions bound to specific HTTP verbs.", ex:"@GetMapping(\"/users/{id}\") public User getUser(@PathVariable Long id) { ... }", code:true},
{q:"Difference between @PathVariable and @RequestParam?", a:"@PathVariable extracts values embedded in the URL path (/users/{id}). @RequestParam extracts values from query string parameters (/users?id=5)."},
{q:"What does @RequestBody do?", a:"Tells Spring to convert the incoming HTTP request's JSON body into a Java object automatically."},
{q:"How do you handle exceptions globally in a Spring Boot REST API?", a:"Using @ControllerAdvice (or @RestControllerAdvice) combined with @ExceptionHandler methods, centralizing error handling instead of repeating try-catch in every controller and returning consistent error responses.", ex:"@RestControllerAdvice class GlobalHandler { @ExceptionHandler(NotFoundException.class) ResponseEntity<?> handle(NotFoundException e){ ... } }", code:true},
{q:"What does @Valid do?", a:"Triggers bean validation (like @NotNull, @Size) on a request body automatically, rejecting invalid input before it reaches business logic."},
{q:"What is the difference between HTTP status 200, 201, and 204?", a:"200 = OK, general success (typically GET). 201 = Created, a successful POST that created a resource. 204 = No Content, successful request with nothing to return, common for DELETE."},
{q:"What is HATEOAS?", a:"An advanced REST principle where responses include links to related actions/resources, letting clients navigate the API dynamically rather than hardcoding URLs."},
{q:"What is idempotency, and which HTTP methods are idempotent?", a:"An idempotent operation gives the same result no matter how many times it's repeated. GET, PUT, and DELETE are idempotent; POST is not (repeated calls typically create duplicate resources)."},
{q:"What is JPA?", a:"Java Persistence API — a specification for mapping Java objects to database tables. Hibernate is the most common implementation of this specification."},
{q:"What is Hibernate?", a:"An ORM (Object-Relational Mapping) framework that lets you work with database rows as Java objects rather than writing raw SQL for every operation."},
{q:"What is the N+1 query problem, and how do you fix it?", a:"Fetching a list of parents triggers one query for the list, then one extra query per parent for related data — N extra queries. Fixed with JOIN FETCH in JPQL, @EntityGraph, or batch fetching to reduce it to fewer queries."},
{q:"Difference between @OneToMany and @ManyToOne?", a:"@OneToMany sits on the 'parent' side (one Order has many OrderItems). @ManyToOne sits on the 'child' side (many OrderItems belong to one Order); they're usually used together to map both directions of the relationship."},
{q:"Difference between lazy loading and eager loading?", a:"Lazy loading fetches related data only when it's accessed — better performance but can throw LazyInitializationException if accessed outside a session. Eager loading fetches related data immediately with the parent, simpler but potentially wasteful."},
{q:"What does @Transactional do?", a:"Marks a method so all database operations inside it run as a single transaction — if anything fails, everything rolls back, keeping data consistent."},
{q:"What happens with @Transactional if an exception occurs?", a:"By default, Spring rolls back the transaction only for unchecked (RuntimeException) exceptions, not checked ones, unless rollbackFor is explicitly configured."},
{q:"Difference between save() and saveAndFlush() in Spring Data JPA?", a:"save() queues the change, which Hibernate may batch and execute later. saveAndFlush() immediately sends the SQL to the database — useful when a following operation needs to see the change right away."},
{q:"What is Spring Data JPA, and how do repositories work?", a:"A Spring module letting you build a data-access layer just by defining an interface (extends JpaRepository<User, Long>) — Spring auto-generates the implementation with common CRUD methods, plus custom queries derived from method names like findByEmail().", ex:"public interface UserRepository extends JpaRepository<User, Long> { Optional<User> findByEmail(String email); }", code:true},
{q:"Difference between JPQL and native SQL queries?", a:"JPQL operates on Java entity objects and fields (database-independent). Native SQL is raw, database-specific syntax used when you need features JPQL can't express."},
{q:"What is Spring Security?", a:"A framework handling authentication (who are you?) and authorization (what are you allowed to do?) for Spring applications, including login flows, password encoding, and access control."},
{q:"Difference between authentication and authorization?", a:"Authentication verifies identity — 'are you really who you say you are' (e.g., login). Authorization checks permissions — 'are you allowed to do this' (e.g., only admins can delete users)."},
{q:"What is the Spring Security Filter Chain?", a:"A series of filters every HTTP request passes through before reaching your controller — each handles one concern (checking a token, CSRF, authentication) in a defined order."},
{q:"What is JWT (JSON Web Token), and why use it?", a:"A compact, digitally-signed token carrying user identity/claims. Since it's self-contained and verifiable without a database lookup, it's popular for stateless authentication, scaling well across multiple servers or microservices."},
{q:"Difference between session-based and token-based (JWT) authentication?", a:"Session-based: server stores session state and gives the client a session ID cookie — requires shared session storage across servers. Token-based: server issues a signed token containing user info; the server doesn't need to remember anything since the token itself proves identity each request (stateless)."},
{q:"How does a typical JWT authentication flow work?", a:"User logs in with credentials, the server verifies them and issues a signed JWT, the client stores it and sends it in the Authorization: Bearer header on every request, and the server verifies the signature and expiry on each request without a database session lookup."},
{q:"What is password encoding, and why never store plain text passwords?", a:"Passwords should always be hashed (e.g., BCrypt) before storing, so even a database breach doesn't expose readable passwords. BCrypt is preferred because it's deliberately slow (resisting brute-force) and includes automatic salting."},
{q:"What does @PreAuthorize do?", a:"A method-level security annotation checking a condition (e.g., @PreAuthorize(\"hasRole('ADMIN')\")) before allowing a method to execute."},
{q:"What is CSRF, and how does Spring Security protect against it?", a:"Cross-Site Request Forgery — an attack tricking a logged-in user's browser into submitting unwanted requests. Spring Security defends with CSRF tokens required on state-changing requests, though it's often disabled for stateless JWT APIs since there's no session cookie to exploit."},
{q:"What is CORS, and why does it matter for full stack apps?", a:"Cross-Origin Resource Sharing — a browser security feature blocking a frontend on one origin (e.g., Angular on port 4200) from calling a backend on a different origin (port 8080) unless the backend explicitly allows it via CORS headers."},
{q:"How do you configure CORS in Spring Boot?", a:"Either with @CrossOrigin on a controller, or globally by defining a CorsConfigurationSource bean specifying allowed origins, methods, and headers.", ex:"@CrossOrigin(origins = \"http://localhost:4200\")", code:true},
{q:"What is a UserDetailsService?", a:"An interface you implement to tell Spring Security how to load user information (username, password, roles) from your data source, usually a database, for authentication."},
{q:"What is AuthenticationManager in Spring Security?", a:"The component responsible for actually verifying credentials during login, delegating to one or more AuthenticationProviders to check the username/password."},
{q:"What is role-based access control (RBAC)?", a:"An authorization model where permissions are assigned to roles (ADMIN, USER) and users are assigned roles, instead of managing permissions per individual user, which doesn't scale."},
{q:"How would you secure a REST API used by both a web frontend and mobile apps?", a:"Typically with stateless JWT authentication — no server-side session, tokens sent via Authorization headers, refresh tokens for renewing expired access tokens, and role/permission claims embedded in the token for authorization checks."},
{q:"What is OAuth2, and how does it differ from basic authentication?", a:"An authorization framework letting a user grant limited access to their data to a third-party app without sharing their password, e.g., 'Login with Google.' More complex than basic auth but far more secure and standard for delegated access."},
{q:"How would you improve the performance of a slow REST API?", a:"Check and add proper database indexes, fix N+1 queries, add caching for frequently-read data, use pagination for large result sets, optimize serialization, and profile to find the actual bottleneck instead of guessing."},
{q:"What is caching, and how would you implement it in Spring Boot?", a:"Storing frequently-accessed data in fast memory (like Redis or an in-memory store) instead of hitting the database every time. Spring Boot supports this via @Cacheable and @CacheEvict annotations plus a caching provider.", ex:"@Cacheable(\"products\") public Product getProduct(Long id) { ... }", code:true},
{q:"What is connection pooling, and why does it matter?", a:"Instead of opening a new database connection per request (slow and resource-heavy), a pool of reusable connections is kept ready. Spring Boot uses HikariCP by default, which is fast and lightweight."},
{q:"How do you version a REST API?", a:"Common approaches: URL versioning (/api/v1/users), header versioning (a custom version header), or content negotiation. URL versioning is simplest and most widely used in practice."},
{q:"What is API rate limiting, and why is it needed?", a:"Restricting how many requests a client can make in a time window, protecting the backend from being overwhelmed by mistake or abuse, and ensuring fair usage across clients."},
{q:"What is Swagger/OpenAPI used for?", a:"A specification and toolset for documenting REST APIs — automatically generating interactive documentation showing endpoints, parameters, and response shapes, which helps cross-team integration."},
{q:"What is the difference between @Component and @Bean?", a:"@Component is a class-level annotation for classes you own, auto-detected by component scanning. @Bean is a method-level annotation inside @Configuration, used when you need custom instantiation logic or are wiring a third-party class you can't annotate."},
{q:"What is Spring Boot Actuator?", a:"A Spring Boot module exposing production-ready endpoints (like /health, /metrics, /info) for monitoring and managing a running application without writing that code yourself."},
{q:"What is the purpose of @ConfigurationProperties?", a:"Binds a group of related external properties (from application.yml) directly into a strongly-typed Java object, instead of injecting individual values one by one with @Value."},
{q:"What is the difference between @Value and @ConfigurationProperties?", a:"@Value injects a single property value into a field. @ConfigurationProperties binds a whole block of related properties into a structured object — cleaner when you have many related settings."},
{q:"What is dependency injection's role in unit testing a Spring service?", a:"Because dependencies are injected (usually via constructor), you can easily substitute real dependencies with mocks in a test, isolating the class under test without needing a real database or external service.", ex:"@Mock UserRepository repo; @InjectMocks UserService service;", code:true},
{q:"What is the purpose of @Qualifier?", a:"Used alongside @Autowired when multiple beans of the same type exist, to specify exactly which bean implementation should be injected."},
{q:"What is a DTO (Data Transfer Object), and why use one instead of exposing entities directly?", a:"A DTO is a plain object shaped specifically for what an API needs to send or receive, decoupled from the database entity. It avoids exposing internal database structure, prevents lazy-loading issues in serialization, and lets the API contract evolve independently of the schema."},
{q:"What is the difference between a monolithic Spring Boot app and a Spring Boot microservice?", a:"A monolith bundles all functionality into one deployable unit sharing one database. A microservice is a small, independently deployable Spring Boot app focused on one business capability, usually with its own database, communicating with other services over the network."},
{q:"How does Spring Boot handle embedded servers?", a:"Spring Boot bundles an embedded servlet container (Tomcat by default, or Jetty/Undertow) directly inside the runnable jar, so you don't need to separately install and configure an external application server."},
{q:"What is the purpose of a health check endpoint in a production Spring Boot app?", a:"Lets load balancers, orchestrators (like Kubernetes), and monitoring tools automatically detect whether an instance is healthy and should keep receiving traffic, or is failing and should be restarted/removed."},
{q:"What is optimistic locking, and how is it implemented in JPA?", a:"A concurrency control strategy that avoids locking rows for reads; instead, a version column is checked on update, and if another transaction changed the row in between, the update fails — implemented in JPA with @Version."},
{q:"What is pessimistic locking, and when would you use it over optimistic?", a:"Pessimistic locking actually locks a database row when read, preventing other transactions from modifying it until the lock is released. Used when conflicts are frequent and you'd rather block than retry, e.g., processing a limited-inventory purchase."},
{q:"What is the difference between @RequestParam(required=false) and using Optional<String> as a parameter?", a:"@RequestParam(required=false) allows the parameter to be absent, defaulting to null unless a defaultValue is set. Wrapping the parameter type in Optional<> similarly allows absence but forces explicit handling of the missing case in code."},
{q:"How do you handle file uploads in a Spring Boot REST API?", a:"Using MultipartFile as a controller parameter, which Spring automatically populates from a multipart/form-data request, then you save or process the file's bytes as needed.", ex:"@PostMapping(\"/upload\") public String upload(@RequestParam(\"file\") MultipartFile file) { ... }", code:true},
{q:"What is the purpose of a Data Access Object (DAO) or Repository layer?", a:"Separates database access logic from business logic, so services don't directly deal with SQL/queries — makes code more testable and lets you swap the underlying data source with minimal impact elsewhere."},
{q:"What is the difference between synchronous and asynchronous method calls in Spring, and how do you trigger async execution?", a:"Synchronous calls block the calling thread until the method completes. Asynchronous calls return immediately while the work continues in another thread, marked with @Async — useful for long-running, non-blocking tasks like sending emails."},
{q:"What does @EnableAsync do?", a:"Enables Spring's asynchronous method execution capability, required before @Async annotated methods will actually run on a separate thread instead of synchronously."},
{q:"What is a Circuit Breaker library commonly used with Spring Boot microservices?", a:"Resilience4j (the modern replacement for the now-deprecated Netflix Hystrix) — it wraps calls to external services and 'opens the circuit' after repeated failures, failing fast instead of waiting on a struggling dependency."},
{q:"What is the purpose of Spring Boot's DevTools?", a:"A development-time dependency that enables automatic restarts on code changes and other developer conveniences, and is automatically excluded from production builds."},
{q:"What is a ResponseEntity used for in Spring?", a:"Represents the full HTTP response — status code, headers, and body — giving you full control instead of just returning a plain object that Spring wraps with defaults.", ex:"return ResponseEntity.status(HttpStatus.CREATED).body(newUser);", code:true},
{q:"What is the difference between @ExceptionHandler at the controller level vs @ControllerAdvice?", a:"@ExceptionHandler on a controller only catches exceptions thrown within that specific controller. @ControllerAdvice combined with @ExceptionHandler applies globally across all controllers, centralizing error handling."},
{q:"What is method-level security vs URL-level security in Spring Security?", a:"URL-level security restricts access based on the request path (e.g., only ADMIN can hit /admin/**), configured in the security filter chain. Method-level security (like @PreAuthorize) restricts access to specific methods regardless of how they're called, giving finer-grained control."},
{q:"What is the purpose of a refresh token in JWT-based authentication?", a:"Access tokens are usually short-lived for security. A longer-lived refresh token lets the client obtain a new access token without forcing the user to log in again, balancing security with user convenience."},
{q:"How would you handle validation errors and return a clean error response to the client?", a:"Use @Valid with bean validation annotations on the request DTO, then catch MethodArgumentNotValidException in a @ControllerAdvice, extracting field errors into a structured JSON response with field names and messages."},
{q:"What is the purpose of pagination in a REST API, and how is it typically implemented in Spring Data JPA?", a:"Prevents returning huge result sets in one response, which is slow and memory-heavy. Spring Data JPA supports it natively via the Pageable parameter and Page<T> return type.", ex:"Page<User> findAll(Pageable pageable);", code:true},
{q:"What is the difference between findById() returning Optional<T> vs just T in older code?", a:"Returning Optional<T> forces callers to explicitly handle the 'not found' case instead of risking a silent NullPointerException later if they forget to check for null."},
{q:"What is the purpose of a service layer between controllers and repositories?", a:"Keeps business logic separate from both the HTTP-handling controller layer and the raw data-access repository layer, making each layer more focused, testable, and reusable across different entry points (REST, batch jobs, etc.)."},
{q:"What is bean validation, and what are some common annotations?", a:"A standard (JSR 380) way to declare validation rules directly on model fields. Common annotations: @NotNull, @NotBlank, @Size, @Min, @Max, @Email, @Pattern.", ex:"@NotBlank private String email; @Min(18) private int age;", code:true},
{q:"How does Spring Boot decide which embedded server to use if multiple are on the classpath?", a:"It uses whichever web starter dependency you included — spring-boot-starter-web defaults to Tomcat; you'd exclude Tomcat and add spring-boot-starter-jetty or -undertow to switch."},
{q:"What is the purpose of the @Async annotation's return type when you need a result back?", a:"You return a CompletableFuture<T> (or similar) so the caller can later retrieve the result once the asynchronous work finishes, instead of Runnable-style fire-and-forget."},
{q:"What is a common way to handle configuration secrets (like DB passwords) securely in a Spring Boot app?", a:"Avoid hardcoding them in application.yml committed to source control; use environment variables, a secrets manager (like AWS Secrets Manager), or externalized config servers, injecting them at runtime instead."},
{q:"What is the purpose of Spring's @Scheduled annotation?", a:"Runs a method automatically on a fixed schedule (fixed rate, fixed delay, or cron expression) without needing an external job scheduler for simple periodic tasks.", ex:"@Scheduled(cron = \"0 0 * * * *\") public void hourlyJob() { ... }", code:true},
{q:"What is the difference between a 401 and a 403 HTTP status code?", a:"401 Unauthorized means the request lacks valid authentication credentials at all. 403 Forbidden means the credentials are valid, but the authenticated user doesn't have permission for this specific action."},
{q:"What is the purpose of the OncePerRequestFilter class in Spring Security?", a:"A base class for custom filters (e.g., a JWT validation filter) that guarantees the filter's logic runs exactly once per request, even in complex dispatch scenarios like forwards or includes."},
{q:"How would you test a Spring Boot REST controller without starting the full application context?", a:"Using @WebMvcTest, which loads only the web layer (controllers, filters) with mocked service dependencies via @MockBean, making tests much faster than a full @SpringBootTest."},
{q:"What is the purpose of @SpringBootTest vs @WebMvcTest vs @DataJpaTest?", a:"@SpringBootTest loads the full application context for integration tests. @WebMvcTest loads only the web layer. @DataJpaTest loads only JPA-related components with an in-memory test database — each scopes tests to just what's needed, keeping them fast and focused."},
{q:"What is the purpose of using MockMvc in Spring Boot testing?", a:"Lets you simulate HTTP requests to controllers and assert on the response (status, body, headers) without starting an actual running server."},
{q:"How would you design an endpoint to support both filtering and pagination for a large dataset?", a:"Accept optional query parameters for filter criteria (e.g., status, dateRange) alongside a Pageable parameter, build a dynamic query (e.g., with Specifications or a custom query), and return results wrapped in a Page<T> with metadata about total results and pages."},
{q:"What is the purpose of Spring Boot's spring-boot-starter dependencies?", a:"Curated dependency bundles (like spring-boot-starter-web or spring-boot-starter-data-jpa) that pull in commonly-needed libraries together with compatible versions, avoiding manual dependency version management."},
{q:"What is the difference between a stateless and stateful web application, and why do most modern Spring Boot APIs aim to be stateless?", a:"A stateful app stores session data server-side between requests. A stateless app treats each request independently, with all needed context (like a JWT) sent by the client each time. Stateless design scales horizontally much more easily since any server instance can handle any request."},
{q:"How would you explain integrating a government-mandated biometric authentication API in a Spring Boot backend during an interview?", a:"Describe the actual flow: the frontend/device captures biometric data, sends it securely to the external authentication service via a REST call from your backend, the backend validates the response, then issues a session/JWT and proceeds with the transaction — emphasizing encryption in transit, error handling for failed matches, and how you scaled it for large user volumes."}
];
