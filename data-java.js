const javaData = [
{q:"What is Object-Oriented Programming (OOP)?", a:"A way of writing programs by grouping data and the actions on that data into objects, like a Customer object holding a name with a method to update it. It makes large programs easier to organize, reuse, and maintain.", ex:"class Customer { String name; void updateName(String n) { name = n; } }", code:true},
{q:"What are the 4 pillars of OOP?", a:"Encapsulation (hiding internal details), Inheritance (reusing a parent class's code), Polymorphism (same method behaving differently per object), and Abstraction (showing only what's needed)."},
{q:"What is encapsulation?", a:"Wrapping data and the methods that use it into one class, controlling access with private fields and public getters/setters so outside code can't corrupt internal state.", ex:"private double balance; public void withdraw(double amt){ if(amt<=balance) balance-=amt; }", code:true},
{q:"What is inheritance?", a:"When one class (child) reuses fields and methods of another class (parent) using extends, avoiding duplicate code.", ex:"class SavingsAccount extends Account { }", code:true},
{q:"What is polymorphism? Give an example.", a:"The same method call behaves differently depending on the actual object type, decided at runtime. This lets you write code against a general type and get specific behavior.", ex:"Shape s = new Circle(); s.draw(); // calls Circle's draw()", code:true},
{q:"What is abstraction?", a:"Hiding complicated implementation details and exposing only essential features to the user of a class or method."},
{q:"Difference between abstract class and interface?", a:"An abstract class can mix finished and unfinished methods and hold instance state; a class extends only one. An interface declares behavior a class promises to implement; a class can implement many interfaces. Since Java 8, interfaces can have default methods but still can't hold instance state."},
{q:"Can an abstract class have a constructor?", a:"Yes — it runs when a subclass object is created, typically to initialize fields shared across all subclasses."},
{q:"What is method overloading?", a:"Defining multiple methods with the same name but different parameter lists in the same class. Resolved at compile time.", ex:"void add(int a,int b){} void add(double a,double b){}", code:true},
{q:"What is method overriding?", a:"A subclass provides its own version of a method already defined in its parent, with the identical signature. Resolved at runtime — this is what enables polymorphism."},
{q:"Difference between overloading and overriding?", a:"Overloading is same class, different parameters, decided at compile time. Overriding is parent-child classes, same signature, decided at runtime."},
{q:"What is a constructor?", a:"A special method with the class's name and no return type that runs automatically when an object is created, typically to set initial field values."},
{q:"What is the 'this' keyword used for?", a:"Refers to the current object — commonly used to distinguish an instance field from a constructor/method parameter with the same name.", ex:"this.name = name;", code:true},
{q:"What is the 'super' keyword used for?", a:"Refers to the parent class — used to call the parent's constructor with super() or a parent's overridden method with super.methodName()."},
{q:"Difference between == and .equals()?", a:"== compares references (are these the exact same object in memory?). .equals() compares actual content/value. For meaningful object comparisons, always override equals()."},
{q:"Why override hashCode() when you override equals()?", a:"HashMap and HashSet use hashCode() first to find the right bucket, then equals() to confirm a match. If only equals() is overridden, two 'equal' objects can land in different buckets and the collection silently breaks."},
{q:"Difference between a class and an object?", a:"A class is the blueprint (design); an object is an actual instance built from that blueprint at runtime."},
{q:"What are Java's access modifiers?", a:"private (class only), default/package-private (same package), protected (package plus subclasses), and public (accessible everywhere)."},
{q:"Difference between static and instance methods?", a:"Static methods belong to the class and are called without an object (ClassName.method()). Instance methods belong to a specific object and require object.method()."},
{q:"Can a static method access instance variables directly?", a:"No — static methods run without any specific object in context, and instance variables belong to a particular object."},
{q:"What does the final keyword do?", a:"final variable: value can't change after assignment. final method: can't be overridden by subclasses. final class: can't be extended at all (String is a final class)."},
{q:"Why is String immutable in Java?", a:"Once created, a String's value never changes; any 'modification' creates a new object. This makes Strings safe to share across threads, safe as HashMap keys, and lets the JVM cache/reuse identical literals for memory efficiency."},
{q:"Difference between String, StringBuilder, and StringBuffer?", a:"String is immutable. StringBuilder is mutable and fast but not thread-safe — best for single-threaded loops building text. StringBuffer is mutable and synchronized (thread-safe) but slightly slower."},
{q:"What is the String constant pool?", a:"A special memory area where Java caches String literals so identical string values are reused instead of duplicated, saving memory."},
{q:"What is autoboxing and unboxing?", a:"Autoboxing automatically converts a primitive (int) to its wrapper object (Integer); unboxing converts it back. It happens implicitly, e.g., when adding an int to a List<Integer>.", ex:"List<Integer> list = new ArrayList<>(); list.add(5); // autoboxed to Integer", code:true},
{q:"What is the Collections Framework?", a:"A set of ready-made interfaces and classes (List, Set, Map, and their implementations) for storing and managing groups of objects, so you don't build data structures from scratch."},
{q:"Difference between List, Set, and Map?", a:"List: ordered, allows duplicates (ArrayList, LinkedList). Set: no duplicates (HashSet, TreeSet). Map: unique keys mapped to values (HashMap, TreeMap)."},
{q:"Difference between ArrayList and LinkedList?", a:"ArrayList uses a resizable array — fast random access by index but slower middle inserts/deletes since elements shift. LinkedList uses linked nodes — slower random access but faster inserts/deletes once you're at the right position."},
{q:"How does HashMap work internally?", a:"It stores key-value pairs in buckets based on the key's hashCode(). Lookup computes the hash, jumps to that bucket, then uses equals() to find the exact match. Since Java 8, an overloaded bucket converts to a balanced tree for better worst-case performance."},
{q:"Difference between HashMap and Hashtable?", a:"HashMap isn't synchronized and allows one null key. Hashtable is synchronized (thread-safe) but slower and disallows null keys/values. Modern code uses ConcurrentHashMap instead of Hashtable for thread safety."},
{q:"What is ConcurrentHashMap and why use it?", a:"A thread-safe HashMap that locks only small segments instead of the whole map, letting multiple threads read/write different parts simultaneously without blocking each other."},
{q:"Difference between HashSet and TreeSet?", a:"HashSet gives no ordering guarantee but is faster. TreeSet keeps elements sorted automatically, at the cost of slightly slower operations."},
{q:"Difference between Comparable and Comparator?", a:"Comparable is implemented inside a class to define its single natural sort order via compareTo(). Comparator is a separate class defining custom sort orders via compare(), letting you have multiple different sort strategies for the same class."},
{q:"What is an Iterator?", a:"An object that lets you step through a collection one element at a time and safely remove elements mid-iteration via iterator.remove(), which a plain for-each loop can't do safely."},
{q:"What happens if you modify a list while iterating with for-each?", a:"You get a ConcurrentModificationException. Use Iterator.remove(), or iterate over a copy of the list instead."},
{q:"Difference between fail-fast and fail-safe iterators?", a:"Fail-fast (ArrayList) throws immediately if the collection changes during iteration. Fail-safe (ConcurrentHashMap) iterates over a snapshot so it won't throw, but may not reflect the very latest changes."},
{q:"What is the difference between Queue and Deque?", a:"Queue supports insertion at one end and removal from the other (FIFO). Deque (double-ended queue) supports insertion and removal at both ends, so it can act as both a queue and a stack."},
{q:"What is exception handling?", a:"A mechanism for gracefully dealing with runtime errors using try-catch-finally, so the program doesn't crash unhandled."},
{q:"Difference between checked and unchecked exceptions?", a:"Checked exceptions (like IOException) must be handled or declared with throws — the compiler enforces this. Unchecked exceptions extend RuntimeException and aren't checked at compile time."},
{q:"Difference between throw and throws?", a:"throw actually raises an exception inside a method body. throws is used in a method signature to declare that a method might throw an exception, warning callers to handle it."},
{q:"What does the finally block do?", a:"Code inside finally always runs, whether or not an exception occurred — commonly used to close resources like connections or files."},
{q:"What is try-with-resources?", a:"A cleaner way to work with closeable resources that automatically closes them at the end, even if an exception occurs, without needing an explicit finally block.", ex:"try (FileReader fr = new FileReader(\"f.txt\")) { ... } // auto-closed", code:true},
{q:"Can you have multiple catch blocks for one try?", a:"Yes — catch different exception types separately, ordering from most specific to most general, so each type gets appropriate handling."},
{q:"What is a custom exception and why write one?", a:"A class extending Exception or RuntimeException representing a specific business error (e.g. InsufficientFundsException), making error handling clearer than relying only on generic exceptions."},
{q:"What is a thread?", a:"A separate path of execution within a program that runs concurrently with other threads, letting a program do multiple things at once."},
{q:"Difference between a process and a thread?", a:"A process is an independent running program with its own memory space. A thread is a lightweight execution unit inside a process, sharing memory with other threads of that same process."},
{q:"What does the synchronized keyword do?", a:"Locks a method or code block so only one thread can execute it at a time, preventing race conditions on shared data."},
{q:"What is a race condition?", a:"A bug that occurs when multiple threads access and modify shared data at the same time, producing unpredictable results depending on timing."},
{q:"What is deadlock?", a:"A situation where two or more threads are permanently stuck, each waiting for a resource the other one holds — like two people each waiting for the other to move first."},
{q:"What is ExecutorService?", a:"A higher-level API for managing a pool of worker threads, so tasks are submitted to the pool instead of manually creating and managing individual Thread objects."},
{q:"Difference between Runnable and Callable?", a:"Runnable's run() returns nothing and can't throw checked exceptions. Callable's call() returns a value and can throw exceptions — useful when a background task needs to hand back a result."},
{q:"What does the volatile keyword do?", a:"Ensures a variable is always read from and written to main memory instead of a thread's local cache, so changes made by one thread become immediately visible to other threads."},
{q:"What is a thread pool and why use one?", a:"A managed group of reusable worker threads. Reusing threads avoids the overhead of creating a new thread for every task and controls how many threads run concurrently, preventing resource exhaustion."},
{q:"What are lambda expressions?", a:"A concise way to write a function inline without a full class definition — implementing a functional interface's single method in one line.", ex:"(a, b) -> a + b", code:true},
{q:"What is a functional interface?", a:"An interface with exactly one abstract method (like Runnable or Comparator), which a lambda expression can implement. Marked with @FunctionalInterface."},
{q:"What is the Stream API used for?", a:"Processing collections in a functional, chainable style — filtering, transforming, and collecting data readably instead of writing manual loops.", ex:"list.stream().filter(x -> x > 5).collect(Collectors.toList());", code:true},
{q:"Difference between map() and filter() in streams?", a:"map() transforms each element into something else (e.g., name to uppercase name). filter() selects only elements matching a condition, without transforming them."},
{q:"What is Optional in Java?", a:"A wrapper that may or may not contain a value, used to avoid returning null directly and reduce NullPointerExceptions. Checked with isPresent() or handled with orElse().", ex:"Optional<User> u = repo.findById(id); u.orElseThrow(() -> new NotFoundException());", code:true},
{q:"Difference between intermediate and terminal stream operations?", a:"Intermediate operations (map, filter) return a new stream and are lazy — nothing runs until a terminal operation is called. Terminal operations (collect, forEach, count) trigger actual processing and end the stream."},
{q:"What is method reference syntax?", a:"A shorthand for a lambda that just calls an existing method, using ClassName::methodName instead of writing out the lambda body.", ex:"list.forEach(System.out::println);", code:true},
{q:"What is JVM?", a:"Java Virtual Machine — the engine that runs compiled bytecode, abstracting away the actual OS and hardware so Java programs run the same way anywhere."},
{q:"Difference between JDK, JRE, and JVM?", a:"JVM runs the code. JRE bundles the JVM plus libraries needed to run Java programs. JDK bundles the JRE plus development tools like the compiler and debugger, needed to write and build Java programs."},
{q:"What is garbage collection?", a:"An automatic process where the JVM frees memory used by objects that are no longer referenced anywhere in the program, so developers don't manually free memory."},
{q:"What are heap and stack memory?", a:"Heap stores actual objects and is shared across the entire application. Stack stores method calls and local variables, and each thread has its own separate stack."},
{q:"What causes a memory leak in Java despite garbage collection?", a:"When objects remain referenced somewhere the program no longer logically needs — like entries left in a static collection or an unregistered listener — so the garbage collector can't reclaim them."},
{q:"What is the difference between compile-time and runtime polymorphism?", a:"Compile-time polymorphism is method overloading, resolved by the compiler based on argument types. Runtime polymorphism is method overriding, resolved at runtime based on the actual object type."},
{q:"What is the Singleton pattern?", a:"A design pattern ensuring only one instance of a class exists across the whole application, with a single global access point — often used for configuration managers or connection pools.", ex:"public static synchronized Config getInstance(){ if(instance==null) instance=new Config(); return instance; }", code:true},
{q:"What is the Factory pattern?", a:"A pattern that centralizes object creation logic in a factory method or class instead of scattering 'new' calls throughout code, making it easy to change what gets created without touching client code."},
{q:"What is the Builder pattern?", a:"A pattern for constructing complex objects step by step, especially useful with many optional fields, avoiding constructors with too many parameters.", ex:"Person.builder().name(\"X\").age(30).build();", code:true},
{q:"What is the Circuit Breaker pattern?", a:"A pattern that 'trips' and stops calling a failing service after repeated failures, instead of letting requests keep failing and piling up — gives the failing service time to recover and protects the caller from cascading failure."},
{q:"What is the Observer pattern?", a:"A subject maintains a list of dependents (observers) and automatically notifies them when its state changes — the basis of most event-driven systems."},
{q:"What is Dependency Injection conceptually?", a:"Instead of a class creating the objects it depends on internally, those dependencies are supplied ('injected') from outside — usually via constructor — making classes easier to test and swap implementations for."},
{q:"What is the Strategy pattern?", a:"A pattern that defines a family of interchangeable algorithms and lets you swap which one is used at runtime, instead of hardcoding logic with if-else chains."},
{q:"What is the Decorator pattern?", a:"A pattern that lets you add new behavior to an object dynamically by wrapping it in decorator objects, instead of modifying the original class or using inheritance for every combination."},
{q:"What is an interface's default method?", a:"A method in an interface with a body, introduced in Java 8, letting interfaces evolve with new functionality without breaking classes that already implement them."},
{q:"Difference between an array and an ArrayList?", a:"An array has a fixed size set at creation and can hold primitives directly. ArrayList is resizable and only holds objects (autoboxing wraps primitives), with many built-in convenience methods."},
{q:"What is the difference between shallow copy and deep copy?", a:"A shallow copy duplicates an object but its nested object fields still point to the same referenced objects. A deep copy recursively duplicates nested objects too, so the copy is fully independent."},
{q:"What is serialization in Java?", a:"Converting an object into a byte stream so it can be saved to a file, sent over a network, or cached — and deserialization converts it back into an object."},
{q:"What is the transient keyword?", a:"Marks a field to be skipped during serialization — useful for sensitive data (like passwords) or fields that shouldn't or can't be persisted."},
{q:"What is reflection in Java?", a:"The ability to inspect and manipulate classes, methods, and fields at runtime, even ones not known at compile time — used by frameworks like Spring to wire beans dynamically."},
{q:"What is an enum in Java?", a:"A special class type representing a fixed set of constants, e.g., DAY types or STATUS values, providing type safety compared to using plain strings or integers.", ex:"enum Status { ACTIVE, INACTIVE, PENDING }", code:true},
{q:"What is the difference between an interface and an abstract class regarding multiple inheritance?", a:"Java doesn't allow a class to extend multiple classes (avoiding the 'diamond problem'), but a class can implement multiple interfaces, giving a form of multiple inheritance for behavior contracts."},
{q:"What is a marker interface?", a:"An interface with no methods, used only to 'tag' a class with metadata the JVM or a framework checks for, like Serializable."},
{q:"What is the difference between == and equals() for wrapper classes like Integer?", a:"For small cached values (-128 to 127), Integer caches instances so == may work by coincidence. For larger values, == compares references and can fail even for equal values — always use equals() or compare with .intValue()."},
{q:"What is the difference between StringBuilder's append() performance vs String concatenation in a loop?", a:"String concatenation with + inside a loop creates a new String object every iteration (since Strings are immutable), which is slow for many iterations. StringBuilder.append() modifies one mutable buffer in place, which is much faster."},
{q:"What is the purpose of the Object class's toString() method?", a:"Provides a human-readable string representation of an object; overriding it (instead of the default hash-based output) makes debugging and logging much clearer."},
{q:"What does 'programming to an interface, not an implementation' mean?", a:"Declaring variables and parameters using an interface type (like List) rather than a concrete class (like ArrayList), so the underlying implementation can be swapped without changing dependent code.", ex:"List<String> names = new ArrayList<>(); // not ArrayList<String> names", code:true},
{q:"What is the difference between composition and inheritance?", a:"Inheritance models an 'is-a' relationship by extending a class. Composition models a 'has-a' relationship by including another object as a field. Composition is generally preferred for flexibility since it avoids tight coupling to a parent class's implementation."},
{q:"What is the diamond problem, and how does Java avoid it?", a:"It occurs when a class inherits conflicting method implementations from two parents. Java avoids it for classes by disallowing multiple class inheritance; for interfaces with default methods, Java forces the implementing class to explicitly resolve any conflict."},
{q:"What is an anonymous inner class?", a:"A class defined and instantiated in a single expression without a name, typically used to implement an interface or extend a class on the spot for one-off use.", ex:"Runnable r = new Runnable() { public void run(){ System.out.println(\"hi\"); } };", code:true},
{q:"What is a static nested class vs an inner class?", a:"A static nested class doesn't need an instance of the outer class to exist and can't access the outer class's instance members directly. A (non-static) inner class is tied to a specific outer instance and can access its members."},
{q:"What is the purpose of the Comparable interface's compareTo() method?", a:"Defines the natural ordering of objects of a class, used by sorting methods like Collections.sort() when no separate Comparator is provided."},
{q:"What is a WeakReference and when would you use it?", a:"A reference that doesn't prevent its referenced object from being garbage collected. Useful for caches where you want entries to be reclaimable under memory pressure rather than held forever."},
{q:"What is the purpose of Java's try-catch with multi-catch syntax?", a:"Lets you catch multiple unrelated exception types in one catch block using a pipe, avoiding duplicated handling code when the response is identical for several exception types.", ex:"catch (IOException | SQLException e) { log.error(e); }", code:true},
{q:"What is the difference between a HashMap and a LinkedHashMap?", a:"HashMap gives no guarantee on iteration order. LinkedHashMap preserves insertion order (or optionally access order), useful when order matters, like implementing an LRU cache."},
{q:"What is the purpose of the Collections.unmodifiableList() method?", a:"Wraps a list so that any attempt to modify it throws an exception, useful for safely exposing internal collections without letting external code mutate them."},
{q:"What is a var keyword in Java (Java 10+)?", a:"Lets the compiler infer the variable's type from the right-hand side, reducing boilerplate for obvious types, while the variable remains statically typed (not dynamically typed like JavaScript's var).", ex:"var list = new ArrayList<String>(); // inferred as ArrayList<String>", code:true},
{q:"What is the difference between an unchecked cast warning and a ClassCastException?", a:"An unchecked cast warning happens at compile time when generics can't verify type safety (e.g., casting a raw List). A ClassCastException happens at runtime when you actually try to cast an object to a type it isn't."},
{q:"What is the purpose of generics in Java?", a:"Lets classes and methods operate on typed parameters specified at compile time (like List<String>), catching type errors early and avoiding manual casting.", ex:"public <T> T getFirst(List<T> list) { return list.get(0); }", code:true},
{q:"What is type erasure in Java generics?", a:"At runtime, the JVM removes generic type information and treats generic collections as their raw type — this is why you can't do 'new T()' or check 'if (obj instanceof List<String>)' directly."},
{q:"What is a bounded type parameter in generics?", a:"A generic type restricted to extend a specific class or interface, e.g., <T extends Number>, letting you call methods of that bound within the generic code."},
{q:"What is the difference between List<?> and List<Object>?", a:"List<?> (wildcard) can refer to a list of any specific type, but you can't add anything to it except null since the actual type is unknown. List<Object> is specifically a list that holds Object references, and you can add any object to it."},
{q:"What is the purpose of the @Override annotation?", a:"Tells the compiler you intend to override a parent method — if the signature doesn't actually match a parent method, the compiler flags an error, catching typos early."},
{q:"What is the difference between System.out.println and a logging framework like SLF4J?", a:"println writes directly to console with no control over output destination, format, or severity. A logging framework lets you set log levels (DEBUG/INFO/ERROR), route output to files or monitoring systems, and disable verbose logs in production without code changes."},
{q:"What is the purpose of unit testing frameworks like JUnit in Java development?", a:"They let you write automated tests that verify individual pieces of code behave correctly, catching regressions early and giving confidence to refactor without manually re-testing everything by hand."},
{q:"What is mocking, and why use it in unit tests (e.g., with Mockito)?", a:"Mocking replaces real dependencies (like a database or external API call) with fake objects that return controlled responses, so you can test a class's logic in isolation without needing the real dependency to be available.", ex:"when(userRepo.findById(1L)).thenReturn(Optional.of(mockUser));", code:true},
{q:"What is the difference between unit testing and integration testing?", a:"Unit tests check a single class or method in isolation, usually with mocked dependencies. Integration tests check that multiple components (e.g., a service, repository, and real database) work correctly together."}
];
